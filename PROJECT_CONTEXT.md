# FOND ordering PWA

## 2026-09-22 - staging-first release consolidation and tablet identity

Implementation: consolidated FOND around one verified code revision with isolated staging and production operational data. Successful `main` verification now deploys only to `fond-test.mid-point.co.za`; production uses a separate manual `Promote FOND Production` workflow requiring the full commit SHA and a successful staging deployment for that exact revision. SHA mismatches fail instead of producing a misleading green no-op. Customer and staff manifests now use host-specific TEST names and colours. The staff PWA has its own `/staff` start URL and scope, registers the existing safe navigation-only service worker, and staging shows a permanent TEST banner on both the sign-in and queue surfaces. Production retains the normal FOND identity.

Testing: TypeScript passed; all 80 unit/integration tests passed, including exact staging-host identity and lookalike-host rejection; the Next.js production build passed and includes both manifest routes. The complete 40-check desktop/mobile Playwright suite passed against an isolated in-memory database. A focused two-browser manifest check also proved production `FOND Staff` and staging `FOND Staff TEST` names, `/staff` start URL and `/staff` scope. `git diff --check` passed. One earlier browser attempt overlapped the production build and failed because both processes wrote `.next`; the required sequential build and complete browser run passed, so this was local test orchestration rather than an application failure.

Commit/push and merge: implementation and the initial evidence entry were committed and pushed directly to `origin/main` as `631186d2ee75d98204844ea33241a849d2514d42` under Brett's instruction to consolidate and push production; no separate pull request or merge was used. Verify FOND run `35694498459` passed on that exact revision in 2m13s: https://github.com/blendproperty/fond/actions/runs/35694498459.

Deployment/configuration: the new staging-only workflow run `35694666926` passed in 45s and deployed that exact revision to the isolated `/opt/fond-test` environment: https://github.com/blendproperty/fond/actions/runs/35694666926. The separate manually dispatched production promotion first proved the same SHA had a successful staging deployment, then run `35694780172` passed in 48s and promoted it to `/opt/fond`: https://github.com/blendproperty/fond/actions/runs/35694780172. No order, database, provider credential or provider switch was copied between environments.

Live verification: staging and production health endpoints both returned HTTP 200 with ordering active. Staging retained `yoco-sandbox`, its public manifest reported `FOND Midpoint TEST`, and the staff manifest reported `FOND Staff TEST` with `/staff` start URL and scope; a browser inspection showed `STAFF PORTAL · TEST` on the rendered sign-in screen. Production retained in-person payments with online payments and SMS disabled; its staff manifest reported `FOND Staff` with the same `/staff` start URL and scope, and the rendered sign-in showed `STAFF PORTAL` without a TEST label. This proves the release and environment identity are live while their operational settings remain separate. Restaurant real-device installation, named staff access, end-to-end order/payment/collection/delivery UAT and outstanding WhatsApp/provider gates remain open. This evidence follow-up will be promoted as a documentation-only revision so remote `main`, staging and production heads remain aligned.

## 2026-09-21 - replacement food menu ready for promotion

Implementation: transcribed the supplied `FOND_A3_Food_Menu_Landscape.md` into the live ordering catalogue as 63 food rows across 11 food sections. Added Buddha Bowls, Poke Bowls and Wraps; updated names, descriptions, dietary labels, prices and preparation estimates; retained the separately approved coffee, tea, cold drink and smoothie range. Removed legacy food from customer ordering by marking the known old rows unavailable rather than deleting them, preserving historical order/report references. Added an idempotent one-time SQLite migration for existing staging/production databases; it replaces the published food range, preserves beverage rows and unrelated admin-created items, and allows later admin edits to remain authoritative. Updated published sandwich bread/bagel upgrades to R20 and zero-cost vegan swaps to match the supplied menu.

Testing: TypeScript passed; all 71 unit/integration tests passed, including migration replacement, retirement, beverage/custom-item preservation and post-migration edit persistence; the Next.js production build passed. The new customer-menu browser assertion passed on desktop and mobile and proves the Golden Goddess Bowl at R120, the new Buddha Bowls/Wraps sections, the 280g rump and absence of the retired 250g rump. The complete 40-check desktop/mobile browser suite was run against an isolated SQLite database: 39 passed initially, and one unrelated transient `ECONNRESET` in an intercepted promotion request passed on immediate isolated rerun. An earlier non-isolated run reused a capacity-filled local operational database and correctly rejected new orders; this was not a menu regression.

Commit/push: `0d620c97e3d2d832a0d9268b6efb208cfcefd0e6` pushed directly to `origin/main`; no separate PR merge. Verify FOND run 35580055297 succeeded. Deploy FOND run 35580278508 succeeded for the exact same revision, including production and isolated staging; no provider credential or environment change was made. Live verification: both `fond.mid-point.co.za` and `fond-test.mid-point.co.za` health endpoints returned `ok`; both menu APIs returned Golden Goddess Bowl R120, Breakfast Foldover R120, 280g Rump R180, four Wraps, no retired 250g Rump, and 24 retained Coffee rows. The rendered staging customer screen showed the replacement category navigation, updated breakfast items/prices and the complete three-item Buddha Bowls section. No order or payment was created during live verification. Restaurant approval of the transcribed wording, dietary/allergen labels and actual kitchen preparation estimates remains UAT; the menu source's included protein/side choices are currently stated in item descriptions and can be captured in the order note until the product supports required, single-choice option groups. Existing Meta WhatsApp/provider gates and real restaurant-device/order UAT remain open.

This documentation-only evidence follow-up skips CI; the deployed application remains `0d620c9`.

## 2026-09-17 - direct Meta WhatsApp Cloud API implementation

Implementation: added direct Meta Cloud API as FOND's preferred WhatsApp provider while retaining Twilio as a fallback. Super-admin Settings now stores the permanent Meta access token, app secret and webhook verification token in the encrypted credential vault, plus non-secret phone-number/WABA IDs and accepted/ready template names. Added `/api/webhooks/whatsapp` with callback challenge verification and HMAC-SHA256 validation of Meta webhook payloads. Controlled test sends and order notification jobs use Meta whenever it is configured. No provider secret is returned to the browser.

Testing: TypeScript passed, all 70 unit/integration tests, all 38 desktop/mobile browser checks and the production Next.js build passed. New tests prove matching callback challenges, rejection of invalid webhook signatures, acceptance of valid signatures, Meta bearer authentication, phone-number routing, template selection and customer/reference variables. A real handset message remains provider UAT after deployment and complete credential entry. An initial browser run without the workflow's isolated test environment failed because it reused local operational state; rerunning with the documented CI environment passed all 38 checks.

Commit/push: `edb91fae8c5b7b87ee1e9302031c1832360d3157` pushed directly to `origin/main`; no separate PR merge. Verify FOND run 35186732653 succeeded. Deploy FOND run 35186871654 succeeded and deployed the same revision to production and isolated staging. No Meta credential or production-data configuration was performed by deployment.

Live verification and configuration (2026-09-17): both production and staging health endpoints returned HTTP 200 after deployment. The new `/api/webhooks/whatsapp` endpoint is routed on both hosts and returned HTTP 403 for an intentionally incorrect verification token, proving fail-closed behavior. A private verification token was then saved through the staging encrypted credential vault. Meta's live GET challenge returned the exact requested challenge, the callback was accepted, and the `messages` webhook field now shows Subscribed. Meta WhatsApp Manager shows FOND number `+27 60 092 8520` with phone-number ID `1231733603366060` under the Fond account, but its display name is `In review` / `Pending display name review`. A registration attempt with a valid six-digit PIN was rejected by Meta's own GraphQL service with provider error `1675030` (`Query error`, server field exception, `allow_user_retry=false`). This proves the PIN format and FOND callback are not the current blocker; number registration remains incomplete until Meta completes the display-name review or support clears the provider-side state. The disclosed test PIN must not be reused. Outstanding gates: wait for/resolve Meta display-name approval; register the number with a new private PIN; save the staging Meta app secret, permanent access token, correct phone-number/WABA IDs and approved accepted/ready templates; publish the Meta app as required for production webhooks; send and reconcile controlled accepted/ready handset tests; only then enable transactional WhatsApp. Production Meta configuration remains untouched, and no Stor24 number or WABA setting was changed.

## 2026-09-15 - navigation/install release live

Commit/push: 3e923938af231514a4fa3617193dcb216a1db6bb pushed directly to main; no PR merge. Verify FOND #73 succeeded (1m46s job), https://github.com/blendproperty/fond/actions/runs/34947022470. Deploy FOND #27 succeeded (36s job), https://github.com/blendproperty/fond/actions/runs/34947195716. No environment/configuration changes.

Live verification: loaded public production app, observed prominent installation panel above menu and category arrows, clicked Next categories and observed Previous become enabled, opened Install FOND and confirmed iPhone/Android/desktop guidance, then dismissed it. Local build and 28 desktop/mobile browser checks passed, screenshots inspected. No orders or OS installations performed; native OS install and real-device UAT remain open. Provider and recovery gates unchanged. This documentation follow-up skips CI; production application stays at 3e92393.

## 2026-09-15 - category navigation and prominent installation

Implementation: visible previous/next controls scroll the horizontal menu categories with proper disabled edge states; touch scrolling remains native. Arrow keys/Home/End navigate categories and bring selections into view. Installation is moved above the menu into a high-contrast panel with phone/desktop icons and a full-width mobile CTA. Supported browser prompts are invoked directly; otherwise a dismissible device-instructions dialog explains iOS/Android/desktop installation. Installed standalone sessions show an installed state. Promotion popups are deferred while install instructions are open. Browser-specific support checked against MDN's Making PWAs installable guide; actual OS installation is not claimed.

Validation: production build/TypeScript and all 28 Playwright desktop/mobile tests passed. New tests verify horizontal movement, keyboard end-category selection, installation guide and no horizontal page overflow. Existing ordering test now scopes its hero assertion to heading level 1 because installation adds a second Good food heading. Desktop/mobile screenshots inspected. Commit/push and direct-main promotion pending with this checkpoint; deployment and live verification pending. No schema/provider/configuration changes. Existing provider/device/real install and recovery UAT gates remain open.

## 2026-09-15 - promotions deployed and live checked

Commit/push: 2c0ffcd935f4c43084db82bec9629d033ab8e8fb pushed directly to main; no PR merge. Verify FOND #72 succeeded (verify job 1m48s): https://github.com/blendproperty/fond/actions/runs/34946403923. Deploy FOND #26 succeeded (deploy job 37s) for the same revision: https://github.com/blendproperty/fond/actions/runs/34946578237. No provider or environment configuration changes.

Live verification: refreshed the authenticated production admin, opened Marketing & CMS, confirmed App promotions and all three display options via an unsaved new editor, then removed that unsaved row. No draft/publication or image upload was saved in production. Public /api/health reports ok; /api/store still has zero published promotions and onlinePayments false. Customer popup/image interactions were exercised locally on desktop/mobile, not as a real published production campaign. The production feature is deployed; a first approved real promotion/image remains an operator publishing step. Existing restaurant-device, provider and recovery UAT gates remain open as documented below.

This documentation-only follow-up skips CI; the deployed application remains 2c0ffcd. Canonical project context and operating instructions accompany the implementation on main.


## 2026-09-15 - customer app promotion formats

Implementation: Marketing & CMS now offers text banners, image cards with editable text overlays, and dismissible native-dialog popups. Includes raster image upload, button label/category selection, active dates, matching card previews and a working popup preview. Existing draft/publish/restore remains the publication boundary. The customer app refreshes published store content every 60 seconds; only server-selected active promotions appear. At most one popup per browser session (first active popup in list), delayed until outside basket/tracking/confirmation. Close, Escape and backdrop dismissal supported. Browser session storage may persist when browsers restore tabs; without storage, suppression lasts the page lifetime.

Images: new additive promotion_images SQLite table stores uploaded bytes alongside existing backup data. Authenticated bounded multipart upload accepts JPG/PNG/WebP signatures up to 2 MB, refuses SVG/HTML, caps the library at 200 images, and uses random public image URLs with explicit MIME/nosniff headers. No remote-image URL fetching. Removing an image from a draft does not delete its bytes, preserving publication rollback. No new production credentials or volumes required.

Testing: production build and TypeScript passed, 42 unit tests and 26 Playwright desktop/mobile checks passed. New coverage includes schedule timezone normalization, bad formats/categories/image URLs, anonymous upload denial, raster retrieval, rejected SVG, format editor/image upload/preview, popup dismissal/session suppression, basket deferral and category navigation. Desktop editor and mobile image popup screenshots inspected. Browser test images/copy are isolated fixtures, not production promotions.

Commit/push: pending commit with this checkpoint, starting from verified origin/main 8c0ea82 on feature/complete-admin. Merge: direct main promotion under existing authorization, no PR merge. Deployment/configuration: pending CI-gated deployment. Live production verification: pending; no production promotions uploaded or published. Prior Yoco/WhatsApp credential and real provider UAT gates, restaurant device/order UAT, marketing consent and offsite recovery gates remain unchanged.


## 2026-09-15 - BI-style reports promoted and live-verified

Commit/push: 19939ec2c33d6c2278d92da7fb2c3abc92707a52 promoted directly to main, no PR merge. Verify FOND #71 succeeded (verify job 1m44s): https://github.com/blendproperty/fond/actions/runs/34945435351. Deploy FOND #25 succeeded in 53s for the same revision: https://github.com/blendproperty/fond/actions/runs/34945600017. No provider/configuration changes.

Live verification: refreshed the existing authenticated admin browser and opened Reports. Business overview, presets/custom dates, fulfilment filter, four zero KPI cards, chart panels, searchable item detail and source notes loaded. Production contains no completed orders in the selected period; no false slow-mover rows or fabricated data appear. Anonymous overview API returned 401. Desktop populated/empty and mobile populated screenshots were inspected locally; 40 unit and 20 browser checks passed before promotion. No production records or payments were created for testing.

PROJECT_CONTEXT.md and README.md are on main. This documentation-only follow-up skips CI; deployed application remains 19939ec. Previous Yoco/WhatsApp configuration, restaurant UAT and offsite recovery gates remain unchanged. Native dashboard styling is inspired by BI tools; Microsoft Power BI is not embedded and no Yoco sales feed is connected.

## 2026-09-15 - replace basic report with business intelligence dashboard

User correction: the Reports list was not the requested Power BI-style experience. Traced the basic report to cc59e08; the preceding admin release retained it. It ranked available zero-sales menu items as slow movers, even with no completed history. That categorisation was misleading and is removed, including from the compatibility endpoint.

Implementation: responsive native FOND business dashboard, not Microsoft Power BI embedding. Added authenticated overview API, 7/30/90-day/custom date and fulfilment filters, equal-length previous-period comparison, four KPI cards, daily trend, fulfilment doughnut, category bars with item drilldown, order-status bars, hourly demand, searchable/sortable item detail and CSV. Empty states explicitly use real data only. Aggregation uses Johannesburg order-placement dates and completed orders for sales/units; all order statuses are shown separately. Stored purchased line values are used; legacy lines without snapshots are flagged, never reconstructed using current prices. Categories reflect the current catalogue and removed items are retained as uncategorised. No Yoco catalogue/sales feed or payment proof is claimed. No schema migration or production business-data change.

Testing: TypeScript/Next production build, 40 unit tests and 20 Playwright desktop/mobile tests passed. New calculation tests cover date boundaries, previous period, fulfilment filtering, historical prices, legacy missing snapshots and empty ranks; new browser tests cover empty/populated views, search, authentication and mobile overflow. Desktop populated and mobile/empty screenshots generated for visual QA. Synthetic data is local only.

Commit/push and deployment: pending promotion in this checkpoint; record actual GitHub CI, deployment and live read-only verification below after success. Prior provider configuration and restaurant UAT gates remain unchanged. This dashboard does not generate fake sales or propose discounts without evidence.

## 2026-09-15 - admin release deployment and live verification confirmed

Implementation/push: 7afa7e035d819a45d6dbe20e40cc5191f64a8c7e promoted directly to origin/main from feature/complete-admin. No separate PR merge. GitHub Verify FOND #70 succeeded in 1m39s: https://github.com/blendproperty/fond/actions/runs/34944302084. Deploy FOND #24 succeeded for the same revision in 47s: https://github.com/blendproperty/fond/actions/runs/34944442146. Its workflow includes the pre-replacement SQLite backup and serialized deployment. Provider configuration was not changed.

Live read-only verification: the existing signed-in Edge admin session was refreshed and all seven navigation labels appeared without Soon. Customers directory, Marketing & CMS draft/publication controls, Finance metrics/ledger/reconciliation and Settings trading/team/provider controls loaded successfully. Existing menu remained available. Settings explicitly reported Yoco and WhatsApp not configured, with both switches off and no named users yet. Public home returned 200; /api/health returned ordering mode, liveOrdering=true and in-person payments; /api/store returned the expected content with onlinePayments=false. Anonymous /api/admin/manage/settings returned 401. These checks verify served screens and read APIs, not real customer transactions or provider delivery.

Local verification remains 37 unit tests, TypeScript/build, 18 browser tests, Docker build and isolated old-image migration/restart/backup-restore integrity checks. No production customer/profile/payment/message mutation was made. Remaining restaurant UAT, live provider activation, actual staff onboarding, approved business content and offsite recovery gates below stay open. The screenshot's four unfinished admin sections are implemented and deployed; this is not a claim that provider/accounting/POS integrations or every enterprise feature exist.

Canonical handover: PROJECT_CONTEXT.md, README.md and OPERATIONS.md are included on main. This evidence-only commit skips CI to avoid a redundant deployment; production application revision remains 7afa7e0. Remote context presence is checked after push.

## 2026-09-15 - full admin sections and optional hosted Yoco checkout

This checkpoint supersedes earlier descriptions of Customers, Marketing & CMS, Finance and Settings as placeholders. Work starts from freshly verified origin/main 9ff768d on feature/complete-admin. Brett explicitly selected optional Yoco checkout in the release clarification.

Implementation: all seven navigation sections are functional. Added customer profiles/search/import/phone history/archive/consent export; CMS draft/preview/publish/restore and scheduled promotions; receipt/refund ledger, balance guards, CSV and checkout reconciliation references; trading controls/hours/capacity/contact/collection preferences; named staff/manager roles with hashed passwords, expiring/revocable sessions and login throttling. Order details/status history UI, purchased modifier snapshots, mobile navigation labels and visible menu-save errors are included. Reports count completed orders. Existing order idempotency and status concurrency logic remain intact.

Optional hosted Yoco checkout is server-side and fail-closed unless enabled/configured. Trusted order totals, stable provider idempotency keys, redirect allowlisting, raw signed webhook verification, three-minute replay window, amount/currency/mode validation and payment/refund deduplication feed the ledger and staff payment indicators. Sandbox keys require an explicit isolated-staging flag. No browser return can mark an order paid. Uncertain checkouts require reconciliation in Yoco before a second receipt; no unsupported fetch/expire endpoint was invented. No Counter/printer integration. Payment/notification provider credentials were not added to production.

Transactional WhatsApp is a durable queue tied to accepted/ready transitions and driven by staff polling, with bounded retries, visible missing configuration and ambiguous-send states. Marketing messages are not sent by this app; consented contacts can be exported for an approved provider. Finance is an operational gross ledger, not bank settlement/tax invoicing or an automatic refund-issuance service. Search/export limits and operating procedures are documented in README.md and OPERATIONS.md.

Testing: 37 unit tests, TypeScript and Next production build passed; 18 Playwright desktop/mobile tests passed, including new customer, CMS publish, finance receipt, named-role and anonymous-denial flows. Desktop/mobile screenshot inspection completed for Settings. Yoco tests use fixtures/mocked checkout calls, not real provider sandbox transactions. Docker image built; isolated migration test used the previous production image with one synthetic order, VACUUM INTO backup, new-image additive schema migration, restart and separate restored-backup container. Order count stayed one; integrity_check returned ok. No production order/payment/notification was created.

Deployment changes: serialized CI-gated deployment builds then backs up the running SQLite database before container replacement. Backup failure stops promotion. Local migration/restore proof is not an offsite production recovery drill.

Commit/push: implementation and this context are being committed together; promotion is a direct main fast-forward under existing deployment authorization. Merge: no separate PR merge. Deployment/live verification: pending GitHub CI and deployment; record actual results after they finish. No complete-production-readiness claim yet.

Remaining external/operational gates: merchant/domain approval and configured Yoco credentials plus genuine sandbox/live payment/refund verification; configured WhatsApp templates/credentials and real delivery verification; restaurant-approved trading/delivery/refund rules and menu/allergens; actual named operators and real-device/staff UAT; offsite backup/retention and recovery drill. Shared codes remain during transition. CMS publication does not send campaigns. Customer phone matches are not verified identities. Historical snapshots/audit records cannot be reconstructed. No Asana scope.




## 2026-09-15 - repair promotion confirmed

Implementation/push: e334e2a885f36b74d75c09fe22aeb652dcb46750 pushed to main from a clean checkout based on ee2ecfb. No separate PR merge. GitHub Verify FOND #69 succeeded: https://github.com/blendproperty/fond/actions/runs/34941661855. Deploy FOND #23 succeeded for that same revision in 44 seconds: https://github.com/blendproperty/fond/actions/runs/34941863657. No deployment configuration change.

Live verification after deployment: HTTPS home, /admin, /api/menu and /api/health returned 200; DB-backed health reported ordering mode, liveOrdering=true and in-person payments. Unauthenticated /api/staff/orders returned 401. No live order, WhatsApp delivery or restaurant staff UAT performed. Earlier operational/management-dashboard gates remain. This evidence-only follow-up is pushed with CI skipped to avoid another application deployment; deployed app revision stays e334e2a. Canonical context presence on origin/main verified after push.

## 2026-09-15 - repair browser submission regression after modifiers

Diagnosis: current remote ee2ecfb restored server reliability and passes all 26 unit tests, but Verify FOND run 34939419750 (attempt 2, job 104289455894) failed both customer browser scenarios. Reproduced locally: both customer and manual staff clients omitted the required Idempotency-Key after the modifiers UI replacement. Staff queue handling had also reverted to optimistic updates. Skipped Deploy FOND jobs were the intended CI gate, not separate deploy failures.

Implementation: restored client submission-key reuse/clear behavior and synchronous double-submit guards on both ordering screens; restored server-confirmed staff transitions, expectedStatus and visible queue errors. Preserved modifier selection, reports and filtering. Added desktop/mobile regression coverage for a lost successful HTTP response followed by an unchanged retry, and manual staff order submission.

Testing: current remote baseline reproduced 2 failures/8 passes; repaired code passed TypeScript, 26 unit tests, Next production build and 14 Playwright tests. Docker fond:ci-repair built successfully (manifest list 329096b319e3311528a50caebb9b20248146019911325257f2e512ae1e930fca). No environment-specific Playwright executablePath was added. No production customer order or notification sent.

Commit/push: repair and context are committed together and promoted from fresh origin/main using normal Git, not whole-file API replacement from a stale checkout. CI and deployment evidence will be recorded after the run. Merge/promotion: direct main fast-forward intended for this regression repair under existing deployment authorization. Configuration: unchanged. Live functional verification: pending; staff/UAT, provider notification, backup/restore and expanded management dashboard gates remain open. This repair does not implement the previously requested full management dashboard.

## 2026-09-15 — menu modifiers, top/slow-movers report, menu filtering + view switcher

Implementation (three commits, `cc59e08`, `4efaef3`, `12187bd`, all pushed directly to `main` via the GitHub MCP file-write path since this sandbox's local git still has no push credentials for this repo): built on top of the admin/CRM backend and left-nav shell from the previous milestone.

- **Menu modifiers** ("add this / remove this", e.g. "Extra cheese +R15", "No onion" at R0, negative prices allowed for a discount-style removal): `Modifier = {id, name, price}` on `Meal`, persisted as `modifiers_json` on `menu_items` (additive `ALTER TABLE` migration in `src/lib/db.ts`), validated and CRUD'd in `src/lib/menu-store.ts` (`validateModifiers`, `addModifier`, `removeModifier`, and `updateMenuItem`'s existing full-replacement `modifiers` field). New admin routes `POST /api/admin/menu/[id]/modifiers` and `DELETE /api/admin/menu/[id]/modifiers/[modifierId]`; the existing `PATCH /api/admin/menu/[id]` also accepts a full replacement array. `src/lib/menu.ts`'s `CartLine` gained `modifierIds?: string[]`; a new `lineKey()` helper (id + sorted modifierIds) lets the same base item appear twice in one basket with different modifier selections without colliding; `quoteCart()` now validates each line's modifier selection against the live menu and returns `selectedModifiers`/`unitPrice`/`subtotal` per line, still always priced server-side against the DB-backed menu, never trusted from the client.
- **Customer PWA**: meal cards show modifier checkboxes when a meal has any; selecting/deselecting updates the displayed price live; "Add" carries the currently-checked modifiers onto the cart line; the basket drawer and running total now use `quoteCart` output (wrapped in a try/catch for the empty/mid-edit case) instead of the old flat `price × quantity` reduce, which previously ignored modifiers entirely.
- **Staff tablet**: the order queue's line list now resolves and displays each line's selected modifier names alongside the item name and quantity. Staff-entered manual orders (`ManualOrderPanel`) were left without modifier selection UI in this pass — not yet decided whether walk-in orders need it.
- **Admin Reports** (`reports` nav item now live instead of "coming soon"): new `GET /api/admin/reports/top-items`, backed by `getItemSalesStats()` in `menu-store.ts` (sums quantity sold per item id across all non-cancelled orders' `lines_json`). Returns top 10 by units sold and a "slow movers" list (bottom-ranked, restricted to currently-available items so a discontinued item doesn't clutter it). Admin UI (`ReportsAdmin` in `admin-panel.tsx`) shows both lists with one-click "Discount" (prompts for a new Rand price, sets `isSpecial`+`specialPrice`+`specialLabel`) and "Showcase" (marks `isSpecial` with a "Featured" label, no price change) actions, both reusing the existing `PATCH /api/admin/menu/[id]` endpoint. Documented limitation (code comment + on-screen note): only unit counts are tracked, not per-item revenue, because `orders.lines_json` stores the raw requested basket, not a priced-at-time-of-order snapshot — reconstructing revenue per item isn't possible from existing data without a schema change to snapshot prices at order time.
- **Menu admin filtering + views**: `MenuAdmin` gained a text search (name), category filter, availability filter (on-menu / hidden / both), and a "specials only" checkbox, plus a List/Card/Compact view switcher (`ViewMode`, three icon-button toggle in the toolbar) that changes the CSS grid/layout of `MenuRow` without changing its data or behaviour — compact view hides the description line, card view lays items out in a responsive grid instead of a stacked list.

Testing: `npm run typecheck` — clean. `npm test` — 22/22 unit tests passed (unchanged from before this milestone; no new test files were added for modifiers/reports/filtering — this is a gap, flagged below). `npm run build` — passed, all 21 routes compiled including the two new modifier routes and the new reports route.

Push: three commits pushed directly to `main` (no PR) via `mcp__remote-devices__github__push_files`, following the same reasoning as every prior milestone (no local push credentials). Commit `cc59e08` — lib/API layer (menu.ts, db.ts, menu-store.ts, the menu/[id] route plus the three new modifier/report route files). Commit `4efaef3` — component layer (ordering-app.tsx, staff-tablet.tsx, admin-panel.tsx). Commit `12187bd` — globals.css. Deployment: none yet — these commits have not been deployed to the VPS as of this entry; the live site still runs whatever commit was deployed at the end of the previous milestone.

Open items carried forward from this milestone: no automated tests were written for modifiers (add/remove/validation/pricing with modifiers), the reports aggregation, or the new admin filters — existing 22 tests still pass because they don't touch this new surface area; staff manual-order entry has no modifier UI; per-item revenue in Reports is not tracked (units only, documented above); deployment to the VPS and a live-verification pass are still open, as is everything already open from the prior CRM-backend milestone (customer/marketing CMS, finance, settings sections remain honest "not built yet" placeholders — not touched in this pass). The user's request also asked to "do all the items in the CRM complete everything" — this pass built the three concretely-specified items (modifiers, top/slow movers report, menu filtering+views) and directly related admin plumbing; it did not attempt the full unscoped 18-section enterprise spec from `FOND_Full_Backend_Specification.md`, since most of that still depends on business decisions (staff roles, notification providers, marketing scope, budget) that remain unanswered — flagged plainly here rather than silently declared done.

## 2026-09-14 - production promotion verified

Implementation: reliability changes remain those in commit 2e447d9. Merge: PR #5 merged to main as a6cbefc2cbb1ff001abbb21e62481989e6128bfa before this deployment follow-up. Testing: GitHub Verify FOND run 34851759231 succeeded on the merge revision. Deployment/configuration: Deploy FOND run 34851926106 succeeded (39 seconds); no additional configuration changes made in this follow-up. Evidence: https://github.com/blendproperty/fond/actions/runs/34851759231 and https://github.com/blendproperty/fond/actions/runs/34851926106.

Live verification: HTTPS home, /staff, /admin, /api/menu and /api/health returned 200. Health returned status=ok, mode=ordering, liveOrdering=true, payments=in-person; unauthenticated /api/staff/orders returned 401. Browser rendered the FOND home and menu navigation. These are read-only smoke checks, not a live completed order, notification, backup/restore or real-device UAT. All operational gates in the preceding checkpoint remain open except merge, CI and deployment, now verified above.

Commit/push: this documentation-only evidence update is committed to main with CI skipped to avoid a redundant application deployment; remote PROJECT_CONTEXT.md presence checked after push. Production application revision remains a6cbefc.

## 2026-09-14 - reliable order lifecycle review branch (current checkpoint)

This checkpoint supersedes obsolete preview/Yoco descriptions below. Baseline verified against GitHub origin/main a57649e. Current model is guest PWA ordering into FOND's own staff tablet, manual POS capture if needed, payment in person, and no Yoco integration. Admin currently provides menu/specials and order history, not a complete CRM. Individual staff accounts are not implemented.

Implementation: feature/reliable-order-lifecycle adds atomic persistent idempotency for customer/manual orders, browser retry keys and double-submit guards, full-entropy references with a unique index for new references, purchased line snapshots, transactional status audit events, stale-status rejection and server-confirmed tablet updates with visible errors. Staff-only history API records shared-staff-tablet honestly. Health now checks DB access and reports ordering mode. Deployment workflow waits for successful main verification and checks the exact triggering SHA. README reconciled with actual application.

Testing: 26 unit tests passed; Next production build/TypeScript passed; all 10 Playwright desktop/mobile tests passed, including API duplicate/conflict checks, anonymous access rejection, staff/admin separation and lifecycle history. Initial API test failed because Secure cookies are not automatically sent by the HTTP test client; the corrected local test explicitly forwards the server-issued cookie. Docker image fond:reliability built successfully (manifest list sha256:11562fabc0257bbc46ae4a89e82e4f558b8bec430da6cd861d002f9007ca06bf). No Docker runtime or restart/restore test is claimed for this checkpoint.

Commit/push: this checkpoint is included in the feature branch commit; remote presence is verified after push in the task handoff. Merge: not merged. Deployment/configuration: no production changes; workflow execution and migration against a production backup remain unverified. Live production verification: read-only site, hero image and health returned HTTP 200 during the initial review; the live health still reported obsolete preview mode. No live order or message was sent.

Open gates: review/merge and CI evidence; database backup/restore and controlled staff UAT before promotion; named staff roles and access controls; capacity/hours/availability policies; durable notification outbox/retries and provider delivery verification; delivery/payment business rules; customer history/auth design; real-device installation. Historical orders keep old short references and cannot retroactively gain purchased item snapshots or earlier audit events. New retry keys protect unchanged submissions; changed payloads are new requests. Audit history currently has an authenticated API only, no history UI. No complete CRM, automated POS or printer integration is claimed.

## Canonical repository and scope

Repository: https://github.com/blendproperty/fond. Initial remote was empty on 2026-09-11; fresh checkout created in `fond-app` under the Menu Comparison workspace. This file is canonical for the ordering application. The existing menu-voting Sites project is separate and unchanged. No `.openai/hosting.json` exists here; the user explicitly requested Next.js, Docker, Traefik and Hostinger VPS.

## 2026-09-11 — initial scaffold

Implementation: Next.js 16.3.4, React 19.3.0, TypeScript, responsive provisional FOND visual direction. Nine proposed meals across Breakfast/Lunch/Smoothies, integer-cent basket calculations, quantity controls, illustrative collection preferences, demo confirmation and session-only reorder history. PWA manifest, 192/512 PNG icons, install prompt and network-first navigation/offline fallback. No offline order queue. No personal data persisted. API health route; live order POST always returns 503. Separate typed Yoco gateway throws until a supported provider interface is available. Non-root Node 24 Alpine Docker standalone image, configurable existing-network Traefik Compose, and GitHub Actions verification workflow.

Testing: TypeScript check, 3 unit tests and local Next production build passed. Four browser scenarios (desktop/mobile order flow and API/PWA assets) passed after correcting the mobile basket accessible name. Desktop 1440px and mobile 390px screenshots visually inspected. Docker image build passed; local container health endpoint returned preview mode/liveOrdering=false, UID 1001 verified. Compose validated with explicit test-only hostname/network/resolver values, not real VPS configuration. Offline browser coverage and final Docker rebuild are being checked before commit.

Commit/push: pending initial commit and remote verification at this checkpoint. Merge: not applicable, empty repository bootstrap. Deployment/configuration: no Hostinger or DNS changes. Actual VPS, hostname, Traefik network and certificate resolver requested from user and pending. Local container is not production deployment. Live production verification: none.

Open gates: Yoco human confirmation and one real incoming POS order accepted and printed once; production authentication/identity provider, durable order store, server-side pricing and availability, idempotency and payment reconciliation/webhook verification, cancellations/refunds, ordering capacity/hours, approved FOND menu/prices/allergens/CI/photography, real-device installation and staff UAT. No live payments or kitchen orders enabled. Customer login is not implemented; demo history is explicitly session-only. No Asana scope. Graphify found no pre-existing corpus to map in the empty repository; no graph completeness claim.

## Provider evidence

On 2026-09-11 Yoco AI Support stated that external orders into Counter and prep-station printing are not a standard public API integration. User-approved human escalation email and existing-merchant integration Google Form were submitted and verified earlier in the parent project. Await human technical reply. Checkout payments/catalogue access do not establish incoming order acceptance or printing support.

Validation update: all 6 desktop/mobile browser tests passed, including real service-worker offline navigation. Final application production rebuild passed after accessibility fix. Initial commit will bootstrap main; remote is still empty immediately before commit. GitHub CI result remains pending until the push triggers it.

Handoff evidence: implementation commit fdbf27effd1b14ec76ed90dd847b3a27b4918fac pushed successfully to origin/main. Fetch and ls-tree verified canonical PROJECT_CONTEXT.md on origin/main. Final Docker rebuild passed (image manifest 00bdad038dd2babc7f7a4d97c20dc63ffb9c8fccdadd296eebdf5e83a4c3d83c); replacement local container returned healthy preview response. All 3 unit and 6 browser tests passed. GitHub-hosted CI not verified; workflow supplied. No merge required for initial empty-repository bootstrap. Deployment and provider gates above remain open.

## 2026-09-14 — first-party auth and durable order-request store

Provider re-check (gate verification, required before touching src/lib/yoco.ts): searched developer.yoco.com, Yoco's help centre and Yoco's 2026-06-17 "Yoco Next 2026" blog post for any change to the 2026-09-11 finding. Result: no change. developer.yoco.com/api-reference still lists only the read-only Yoco API (fetch orders/payments/payouts) and the Checkout API (create online payment checkouts) — neither accepts a created order for Counter/staff acceptance. The blog's "prep stations" feature is Yoco's own in-house Counter-to-kitchen routing for orders placed inside Counter, not an API for external systems. No printer or order-injection endpoint exists anywhere in the docs, and no partner/marketplace ordering program was found. `src/lib/yoco.ts` therefore still deliberately throws and was not touched. Do not reinterpret this as a green light without a fresh check and a dated source. *(Correction: this finding understated the real API surface — see the two entries below dated later on 2026-09-14. `WebFetch` against developer.yoco.com's rendered docs was silently truncating/summarizing pages and missing whole sidebar sections; browser navigation surfaced far more. Treat single-tool doc research on this site as unreliable going forward — cross-check with an actual browser render before asserting an endpoint doesn't exist.)*

Implementation: added first-party email/password authentication (`src/lib/auth.ts` — scrypt password hashing, 30-day session tokens, HttpOnly cookie `fond_session`) and a durable SQLite store (`src/lib/db.ts`, Node's built-in `node:sqlite`, no native module so the Alpine image needs no compiler stage). New routes: `POST /api/auth/signup`, `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/session`. `POST /api/orders` durably records the requested basket via `src/lib/order-requests.ts` when the requester is signed in (`GET /api/orders` returns that user's history) — but the HTTP response is unchanged for everyone: still `503 ORDERING_NOT_ENABLED`, still no payment taken, no Yoco call, no kitchen order, because the Yoco gate above remains closed. *(Correction, same day: the version originally described here 401'd anonymous requests before returning that 503, which broke the pre-existing e2e contract that anonymous `POST /api/orders` always gets 503 — see the CI-fix entry below. The fail-closed 503 is unconditional; sign-in only gates whether the request is durably recorded.)* This closes the "no durable order store" and "no authentication" gaps at the infrastructure level without claiming live ordering. The customer-facing UI (`src/components/ordering-app.tsx`) was intentionally left on session-only demo state in this pass — wiring sign-in/sign-up screens and switching the basket flow to the new authenticated endpoints is still open, tracked below. Dockerfile now creates `/app/data` (owned by the existing non-root `fond` UID 1001) and sets `FOND_DB_PATH=/app/data/fond.sqlite`; `compose.yaml` adds a named `fond-data` volume mounted there so the database survives redeploys; `.env.example` documents `FOND_DB_PATH`.

Testing: `npm run typecheck` passed with no errors. `npm test` — 10/10 unit tests passed (7 previous + 3 new files: `tests/auth.test.ts`, `tests/order-requests.test.ts`), all against an in-memory SQLite DB (`FOND_DB_PATH=:memory:`), no state leakage between tests. `npm run build` passed. Manual smoke test against the built standalone server confirmed signup/login/logout/session and the persisted-order-request flow.

Push: opened as pull request #1 (`feature/auth-and-durable-order-requests` → `main`) rather than pushed straight to `main`, written via the GitHub API/MCP file-write path since this session's sandboxed git proxy has no push credentials for this repository (`git push` returns 403 "not in this session's authorized repository set").

Deployment: none yet at this point in the day. Live verification: none yet.

## 2026-09-14 (later) — CI fix, merge, first VPS deployment, and CI/CD

Implementation: PR #1's CI failed on both `push` and `pull_request` runs. Root cause: `POST /api/orders` had been changed to return `401 AUTH_REQUIRED` for an unauthenticated caller before ever reaching the fail-closed response, but `e2e/ordering.spec.ts`'s "PWA manifest and fail-closed order API" test posts unauthenticated and asserts `503`/`ORDERING_NOT_ENABLED` — a real regression, not a flaky test. Fix (commit `a028cb8` on the PR branch): the `503 ORDERING_NOT_ENABLED` response is now unconditional for every caller, authenticated or not, valid basket or not; sign-in only gates the *bonus* behaviour of durably recording the request. Reasoning: the one invariant this endpoint must never violate is claiming a real order happened — that must hold regardless of auth state, so auth has no business gating it.

Testing: reran `npm run typecheck` (clean) and `npm test` (10/10) after the fix. Rebuilt with `npm run build` (all 8 routes compiled). Manually started the built standalone server and replicated both code paths: an anonymous `POST /api/orders` with the e2e test's exact payload (`{"amount":1}`) returned `503`/`ORDERING_NOT_ENABLED` with the original message text (no "saved" suffix) — matching pre-existing behaviour exactly; a signed-in request with a valid basket returned the same `503` with the "Your request has been saved" suffix, and `GET /api/orders` returned it back with correct `totalCents` and `blocked_pending_pos_integration` status. Brett confirmed GitHub Actions CI (`Verify FOND`) went green on the PR after the fix.

Push/merge: PR #1 merged into `main` by Brett via the GitHub UI (`merged_at` 2026-09-14T06:50:13Z, merge commit history: 16 commits, +440/-3 across 15 files). Two follow-up commits (workflow cleanup) landed on the feature branch a few minutes *after* the merge and were therefore not included in it; applied directly to `main` afterward (commit `7e3cab9`) once noticed.

CI/CD: added `.github/workflows/deploy.yml` — on push to `main` (or manual dispatch), SSHes into the VPS using three repository secrets Brett added himself in GitHub's Settings → Secrets UI (`VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY` — a dedicated `github-actions-deploy` keypair Brett generated directly on the VPS; this session never generated, saw, or handled the private key, per a hard tool-level restriction on materializing credentials), then `git fetch`/`checkout main`/`reset --hard origin/main` and `docker compose up -d --build` inside the existing `/opt/fond` checkout. First run ("Deploy FOND #1", commit `7e3cab9`) succeeded in 39s per GitHub Actions' own run history — the SSH connection, git update and rebuild all worked end-to-end.

Deployment (manual, same session, before CI/CD existed): built and ran the `feature/auth-and-durable-order-requests` branch directly on the Hostinger VPS (93.127.186.194, `srv938083`) via commands relayed through Brett's own root SSH session (this session's device-bridge shell access was unavailable — broken by an unrelated Windows update — so every command was given to Brett to paste rather than run directly). First attempt on port 8090 via bare `docker run` worked internally (`/api/health` returned healthy over localhost) but was unreachable externally — Hostinger's own cloud firewall (separate from the box's `ufw`) was silently dropping the port. Switched to routing through the VPS's existing shared Traefik instance instead of fighting per-port firewall rules: added a DNS-only (non-Cloudflare-proxied) `fond.mid-point.co.za` A record to 93.127.186.194 as an explicitly-agreed staging subdomain (a dedicated domain to follow later), confirmed the existing cert resolver name (`mytlschallenge`, TLS-ALPN-01 — confirming DNS-only was required, since Cloudflare's proxy would intercept the TLS-ALPN challenge) and entrypoint (`websecure`) by grepping other projects' compose files on the same VPS, set `.env` (`FOND_HOST=fond.mid-point.co.za`, `TRAEFIK_NETWORK=root_default`, `TRAEFIK_ENTRYPOINT=websecure`, `TRAEFIK_CERTRESOLVER=mytlschallenge`, `FOND_DB_PATH=/app/data/fond.sqlite`), then ran `docker compose up -d --build`.

Live verification: `https://fond.mid-point.co.za/api/health` returned `{"status":"ok","application":"fond","mode":"preview","liveOrdering":false}` — confirmed directly by Brett in his own browser (this sandboxed session's own network egress is allowlisted and cannot reach that domain to check it itself). Real TLS certificate issued via `mytlschallenge`. This is the first live, externally-reachable deployment of FOND. It remains explicitly a staging subdomain on the existing `mid-point.co.za` domain, not the final approved FOND hostname.

Open gates (updated 2026-09-14, end of day): Yoco human confirmation and one real incoming POS order accepted and printed once (unchanged, still closed — re-checked earlier today, no provider change); customer-facing sign-up/login UI and wiring the basket flow to the authenticated endpoints (backend ready since this morning, UI still untouched); idempotency and payment reconciliation/webhook verification (nothing to reconcile against without a payment/POS connection); cancellations/refunds; ordering capacity/hours; approved FOND menu/prices/allergens/CI/photography; real-device installation and staff UAT; a dedicated production hostname (currently a `mid-point.co.za` staging subdomain, explicitly agreed as temporary); Playwright browser suite has not been re-run against the deployed build specifically (CI covers it on every push, most recently green); no `noindex`/basic-auth added yet to keep the staging subdomain out of search/crawl before it's ready for real customers — flagged to Brett, not yet actioned. No live payments or kitchen orders enabled. No Asana scope.

## 2026-09-14 (later still) — Yoco domain-verification block found; corrected API research begins

Yoco Checkout API domain verification: Brett's Yoco "Verified Domains" panel shows `fond.mid-point.co.za` as "Unable to review — it looks like your website is unavailable or still being built." No documented technical remedy exists (no meta tag/file/DNS record described anywhere in Yoco's docs or help centre); this reads as a manual/business review gate, most likely triggered by the site currently being a bare staging preview with provisional menu/prices/branding and no real business content, T&Cs or contact info. This does not block building or testing the Checkout API integration against Yoco's test/sandbox keys — it only blocks switching to live keys for real customer payments via Checkout API specifically. Revisit once the site has real menu/branding content worth a human reviewer seeing. *(See the entry below: Payment Links may sidestep this block entirely since they don't involve a merchant-hosted redirect domain.)*

Correction to prior API-surface findings (2026-09-11 and 2026-09-14 morning entries above): those findings were wrong about the extent of Yoco's public API and were produced by fetching developer.yoco.com through a single automated tool that was silently missing whole sections of the rendered docs site (it summarized pages down to "only two APIs exist" when the live site's own sidebar lists far more). Re-verified by driving an actual browser against developer.yoco.com/api-reference and reading its full rendered navigation tree directly. Confirmed real, currently-documented endpoints beyond what was previously found:

- **Catalogue is a real, documented, read-only API** — `GET /v1/items`, `GET /v1/items/:item_id`, plus Item Brands and Item Categories (fetch + list, cursor-paginated, `created_at`/`updated_at` range filters), scope `business/catalogue:read` (deprecated alias `business/products:read`, still accepted). Response includes name, description, `default_price` (amount/currency), barcode, tax, images, variants, modifier groups. **This means FOND's menu can be pulled live from Yoco's own product catalogue** (maintained via the Yoco app or CSV/bulk upload, since there is still no documented *write* endpoint for items/categories/brands) rather than hand-maintained in `src/lib/menu.ts`. Matching webhook events exist too: Item Created/Updated/Deleted, Item Category Created/Updated/Deleted, so a sync can be push-based rather than polled.
- **Orders API is real but still read-only** — `GET /v1/orders`, `GET /v1/orders/:order_id`; an order has state `open`/`completed`/`cancelled`. Webhook events exist for Order Created/Updated/Completed/Cancelled (thin payload: just `business_id`/`order_id`/`event_type` — the full order must be fetched separately). This lets FOND *observe* orders that already exist in Yoco's system in real time, but there is still no POST/create endpoint in the public reference — nothing lets an external system inject a new order with line items for Counter/staff to see. The original core finding stands: **no self-serve API exists for FOND to place an order into Yoco Counter or trigger prep-station/kitchen printing.**
- **New: Web POS API** — `POST /v1/webpos` registers a virtual "device" (e.g. a named station), then `POST /v1/webpos/:webpos_device_id/payments` (scopes `business/webpos:read`+`:write`) initiates a **real, in-person card payment on Brett's actual physical Yoco card machine** — the response includes the real terminal model/serial once a physical terminal picks it up, and status resolves to `successful`/`failed`/`pending`. Payload is amount + a free-text `client_reference`/`metadata` only — no line items — and it requires the customer to be physically present to tap/insert a card on that terminal, so it fits an in-store "order on a tablet at the counter, then tap the card machine" flow, not a remote pay-ahead-from-home flow.
- Also newly confirmed to exist (not yet evaluated for relevance): Locations, Modifier groups, Staff, Card machines (fetch/list existing physical terminals), Capital (merchant cash-advance data), and full OAuth 2.0 token/userinfo/logout endpoints for Yoco Connect-style third-party auth.

Net effect on architecture: the "customer PWA → FOND backend → staff accept → printer" redesign (Yoco used only for payment, FOND owns the order/kitchen state) remains the correct plan. What changes: (1) FOND's menu/pricing can now be sourced live from Yoco's real catalogue API if Brett wants menu management to happen inside the Yoco app; (2) Web POS is a second, real, hardware-integrated payment option worth considering for in-store ordering specifically, alongside Checkout API for remote ordering.

Process note: going forward, treat any "no such Yoco endpoint exists" conclusion as unverified until confirmed by an actual browser render of developer.yoco.com, not by `WebFetch` alone — `WebFetch` against this specific site has now twice produced confidently wrong negative findings.

## 2026-09-14 (full API surface review) — complete pass over every Yoco endpoint relevant to FOND

Following a direct request to review the entire API reference rather than a sample, every endpoint group in the live sidebar was opened and read (not just listed): OAuth API (introduction, authorize/token/userinfo/logout, token-info), and under the Yoco API — Capital, Card machines, Items/Item Brands/Item Categories, Locations, Modifier groups, Orders (incl. full `Order` schema), Payment Links, Payments, Payouts, Refunds, Staff, Web POS, Webhooks (management + all 12 event-type payloads), plus the Checkout API's Checkouts and Webhook Events sections, plus the `/docs` getting-started and security pages. Findings that materially affect FOND:

- **Authentication model, clarified.** Brett (as the business owner) does not need Yoco's partner/OAuth approval process to use any of the self-serve `business/*` scopes. `developer.yoco.com/docs/getting-started/build-with-yoco/yoco-api/personal-applications` documents a direct path: log in to the Yoco Developer Console, create a "personal application", pick only the scopes needed, generate a static API key immediately (shown once). OAuth 2.0 (Authorization Code flow) exists as a separate mechanism only for third-party apps that need to access *other* merchants' Yoco accounts on their behalf (e.g. a SaaS product serving many independent restaurants) — not needed for FOND acting on Brett's own single business. So catalogue sync, orders/payments visibility, webhooks, and Web POS are all available today with a personal-application API key; only *order injection under an external channel* (the Uber-Eats-style mechanism, see below) requires the separate Partnership application.
- **The full `Order` object confirms the actual mechanism behind Uber-Eats-style integrations.** `GET /v1/orders/:id` returns (among other fields) `channel` (free-form string, Yoco's own example value is literally `"uber_eats"`) and `external_id` (the originating system's own order ID, for reconciliation), plus a full `line_items` array (name, quantity, unit price, modifiers, tax, discounts) and a `payments` array showing each payment's `payment_source` (seen values: `payment_link`; `checkout`, `card_machine`, `webpos` are the other plausible enum members though not enumerated on this page) and `card_machine_id`. This proves external systems can and do inject full itemised orders into a merchant's Yoco account (that's how a Deliveroo/Uber-Eats order appears on Counter and prints) — but `developer.yoco.com/partnerships` confirms this capability is only unlocked via Yoco's formal Partner application + an approved OAuth application, not available through a personal-application API key. This is a real, viable path for FOND, just a business-development one (apply, wait for approval) rather than an immediate technical build.
- **Payment Links may be the practical way to accept real payments today, bypassing the domain-verification block.** `POST /v1/payment_links/` (scopes `business/orders:read`+`:write`) takes just an `amount` and a `customer_reference`/`customer_description` and returns a `pay.yoco.com/r/...` URL with status `pending`/`paid`/`cancelled` — no `successUrl`/`cancelUrl`/redirect domain involved at all, unlike Checkout API. Since the Verified-Domains gate that's currently blocking Checkout API live keys is specifically about a merchant-hosted redirect domain, Payment Links likely don't require it (not yet confirmed with Yoco support — flagged as an assumption to verify before relying on it). If confirmed, FOND could: build the order in its own DB → call this endpoint for the total → send the customer the returned pay.yoco.com link (e.g. via the order confirmation screen or SMS/WhatsApp) → poll or webhook (`payment.created`/order webhooks) to detect payment → release the order to staff. This is a real, buildable stand-in for Checkout API while the domain review is stuck, at the cost of a slightly clunkier customer experience (redirected to a generic Yoco-hosted link page rather than an embedded/branded checkout).
- **Checkout API's own schema supports itemisation for display.** `POST /api/checkouts` (`https://payments.yoco.com/api/checkouts`) accepts optional `lineItems`, `totalDiscount`, `totalTaxAmount`, `subtotalAmount`, `metadata`, `externalId` and `clientReferenceId` alongside the required `amount`/`currency` — so once live keys are unlocked, FOND's actual basket can be shown to the customer on Yoco's payment page and reconciled back via `externalId`, not just a bare total. Also supports an `Idempotency-Key` header to prevent duplicate checkout creation on retry — should be used when FOND eventually wires this up. The `payment.succeeded` webhook payload was also confirmed (nested `payload.metadata.checkoutId`, masked card details) — matches the webhook-verification design already planned.
- **Refunds, Payouts, Payments** are all straightforward read-only ledger data (`business/orders:read` scope covers payments/refunds; each references `staff_id`, `order_id`, `payment_id`) — useful later for a reconciliation/reporting view, not needed for the current build.
- **Staff API** (`business/staff:read`) lists Yoco's own staff records (name, role, active/inactive, staff number) — could be used later to map "who accepted this order in FOND" to a real Yoco staff member for reporting, but FOND's own accept-flow doesn't need this to function.
- **Locations, Modifier groups, Card machines** are read-only reference data (multi-location support, item add-on/modifier definitions matching what `line_items[].modifiers` can carry, and the merchant's registered physical terminals respectively) — none block or unlock anything for FOND's core flow; Modifier groups could be worth syncing alongside Items later if FOND ever needs "add cheese"-style options to match Yoco's own catalogue.
- **Webhook subscriptions are self-serve too** — `POST /v1/webhooks/subscriptions/` (scopes `application/webhooks:read`+`:write`) takes a `notification_url` and a list of event types and returns a signing `secret` shown once, standard HMAC-verification pattern, matching what's already planned for the Checkout API webhook.

Updated recommendation: (1) generate a personal-application API key in Brett's Yoco Developer Console scoped to `business/catalogue:read`, `business/orders:read`, `business/webpos:read`+`:write`, `application/webhooks:read`+`:write` — no approval wait, usable immediately; (2) verify with Yoco support (or by testing in sandbox) whether Payment Links genuinely bypass Verified-Domains, and if so use them as the near-term live-payment path instead of waiting on Checkout API's domain review; (3) submit the Yoco Partnership application in parallel as the real long-term fix for order-injection/kitchen-printing (channel-based orders), while building FOND's own staff-accept-and-print flow now as the thing that works regardless of that application's outcome; (4) still do not touch `src/lib/yoco.ts`'s throwing behaviour until one of these payment paths is actually implemented and tested end-to-end.

## 2026-09-14 (evening) — architectural pivot: Yoco dropped entirely, real menu, facility staff tablet

Decision (Brett, verbatim): "We have decided to kill the Yoco integration and rather go with a PWA tablet at the facility they can add orders manually to system as needed etc." All prior Yoco research above (provider gates, API surface review, Partnership path) is now historical context only — nothing in it is being pursued. Payment happens entirely outside the app from now on: staff take payment in person on the existing card machine or cash at collection; FOND's job is only to track what was ordered and its kitchen/collection status.

Branding decision (Brett, via direct question after reviewing the uploaded `Midpoint_Corporate_Identity_v1.0.pdf`): the CI doc shows FOND is architecturally slated to become "Midpoint Cafe" but flags that rename as explicitly open/unbudgeted; the two uploaded menu PDFs (`FOND Midrand_Main Menu_V4.pdf`, `FOND Midrand_Tapas Menu_V3.pdf`) and current live signage still say "FOND". Offered three options (keep FOND as-is / switch to Midpoint Cafe now / keep FOND name+menu but adopt Midpoint's confirmed design system); Brett chose the third: **FOND name and menu content stay, but the visual system now follows Midpoint's Corporate Identity v1.0 exactly** — palette Ink `#082121` / Signal Aqua `#39EAE6` / Mist `#F3F7F6` / Slate `#6D7280` / Line `#CACDD5` / Paper `#FFFFFF`, plus FOND's own carried-over venue accent Oxblood `#8C3126` / Bone `#EFE7DA`; typeface Figtree (300–800) with documented OS fallbacks; Aqua confined to text/labels on dark Ink backgrounds only per the CI doc's WCAG table (Aqua-on-Paper/Mist "FAILS BADLY" at 1.49:1 and must never be used).

Implementation:
- **Yoco removed entirely**: deleted `src/lib/yoco.ts` (`YocoOrderGateway`), `src/lib/order-requests.ts`, and their test file. No payment provider code remains in the app.
- **Real menu**: `src/lib/menu.ts` rewritten from the two uploaded PDFs — 15 categories (All-Day Breakfast, The Grill, Plates, Burgers, Sandwiches, Salads, Desserts, Pizzas & Foldovers, Tapas, Coffee, Tea & Steamers, Cold Brew & Iced, Smoothies, Cold Bar & Juice, Sides/Sauces/Add-Ons), 100+ real items with real Rand prices and diet tags (vegan/vegetarian/gluten-free), replacing all placeholder demo data.
- **Real order lifecycle**: new `src/lib/orders.ts` and `orders` SQLite table (replacing `order_requests`). States `received → accepted → ready → completed`, with `cancelled` reachable from any non-terminal state; transitions enforced server-side via an explicit map, rejecting invalid moves with a typed `OrderTransitionError`. Two sources: `customer` (public PWA, starts at `received`) and `staff` (typed into the facility tablet for a walk-in/phone order already taken, starts at `accepted`).
- **`POST /api/orders` now really creates orders** (201, no more fail-closed 503) and **`GET /api/orders?reference=`** is a new anonymous status lookup — customer ordering no longer requires an account; a name + optional note is captured directly on the order.
- **Facility staff tablet** (`/staff`, `src/components/staff-tablet.tsx`): a single shared access code (`FOND_STAFF_CODE` env var) gates the whole device via an HMAC-based cookie token (`src/lib/staff-auth.ts`) that never stores the raw code — appropriate for one shared physical device rather than per-staff logins. Staff see a 3-column live queue (New/Preparing/Ready), can advance or cancel any order, and can key in a manual order (category-tabbed item grid, same menu data) for walk-ins/phone orders. New routes: `POST /api/staff/login`, `POST /api/staff/logout`, `GET`/`POST /api/staff/orders`, `PATCH /api/staff/orders/[id]`.
- **Customer PWA** (`src/components/ordering-app.tsx`) rewritten: removed all "preview only"/Yoco-pending copy, added name/note fields to checkout, real `fetch('/api/orders', ...)` on submit, replaced the old session-only "My previews" drawer with a "Track order" drawer backed by the new reference-lookup endpoint, confirmation copy now reads "Order sent to FOND" / "Please pay at the counter on collection."
- **Styling** (`src/app/globals.css`, `src/app/layout.tsx` theme colour): rewritten to Midpoint's design-system tokens end-to-end, including a new `.staff-*` block for the facility tablet UI, careful never to place Aqua as text on Paper/Mist per the CI doc's contrast table.
- `.env.example` updated: removed the Yoco-secrets note, documented `FOND_STAFF_CODE`.

Testing: `npm run typecheck` — clean. `npm test` — 13/13 unit tests passed (existing `auth.test.ts` unchanged; `cart.test.ts` updated to quote real menu item ids; two new files `orders.test.ts` — order creation/status defaults by source, input validation, reference lookup, active-order filtering, valid/invalid status transitions — and `staff-auth.test.ts` — correct/incorrect code, token validation against missing/forged tokens — all against an in-memory SQLite DB). `npm run build` — passed, all 14 routes compiled (new `/staff`, `/api/staff/*` routes present, old order-request routes gone). `e2e/ordering.spec.ts` rewritten for the new flow (real order placement + tracking, updated PWA/API contract assertions, new staff-tablet unlock test) and run locally with Playwright against the production build — **4/4 passed**, confirming the customer order → confirmation → track flow, the PWA manifest/asset contract, offline fallback, and staff-tablet code-gate all work end-to-end.

Push: pending — not yet pushed to GitHub as of this entry (local sandbox has no git push credentials for this repo; will go via the GitHub API/MCP file-write path as PR, consistent with prior milestones).

Deployment: none yet for this milestone. Live verification: none yet.

Open items carried forward: `src/lib/auth.ts` and its four routes are now unused by ordering (customer ordering is anonymous) but were left in place, not removed — not yet decided whether to keep for a future feature or delete; menu-copy tone-of-voice pass against the CI doc's "appetite-first, no chef-speak" guidance not yet done (descriptions were transcribed largely as-is from the PDFs); no dedicated production hostname beyond the existing `fond.mid-point.co.za` staging subdomain; ordering capacity/hours, allergen review and staff UAT on a real tablet device remain open exactly as before.

## 2026-09-14 (night) — PR #2 CI fix, real header photo, and a discovered tool limitation

Implementation: opened the above pivot as pull request #2 (`feature/kill-yoco-staff-tablet` → `main`) via the GitHub API/MCP file-write path (same reason as always — this sandbox's local git has no push credentials for `blendproperty/fond`). Initial CI run failed both `pull_request` and `push` checks ("All checks have failed"). Root cause: `.github/workflows/ci.yml`'s `test:e2e` step didn't set `FOND_STAFF_CODE`/`FOND_DB_PATH`, so the new staff-tablet e2e test's login POST hit `checkStaffCode`'s unconfigured-server guard and got a `503 NOT_CONFIGURED` instead of logging in. Fixed by adding `FOND_STAFF_CODE: ci-test-code` and `FOND_DB_PATH: ':memory:'` to that step's `env:` block. Verified locally with the identical env vars before pushing (all 4 e2e tests passed), then pushed the fix (commit `Fix CI: set FOND_STAFF_CODE/FOND_DB_PATH for the e2e test:e2e step`). Confirmed both `verify` check runs on the PR's head commit now report `conclusion: success`.

Real header image: Brett confirmed the menu should match the two uploaded PDFs (it does — see the pivot entry above) and asked for a real header/hero photo from the "Midpoint Hub" folder he'd referenced earlier. That folder wasn't in this session's initially-connected device folders (only an unrelated `midpoint` project folder was); requested and was granted access to the actual OneDrive `Midpoint Hub` folder on Brett's Windows desktop, found 21 real venue/food photos there, and selected an exterior shot clearly showing "FOND EATERY" and "Midpoint HUB" signage (`WhatsApp Image 2026-09-14 at 11.14.48.jpeg`) as the best header candidate. Optimized it (ImageMagick: resized to 1600px wide, quality 78, metadata stripped, 244,170 bytes final) and pushed the two code changes it needs — `src/components/ordering-app.tsx` (hero `<img>` markup) and `src/app/globals.css` (`.hero-art` restyled for a real photo background with a dark Ink gradient overlay, `.hero-art::after`, plus a mobile min-height bump) — both plain-text changes, pushed successfully via the GitHub MCP tools.

**Tool limitation discovered**: the `mcp__remote-devices__github__push_files`/`create_or_update_file` MCP tools cannot upload true binary files. Verified experimentally: pushed a base64-encoded 1×1 PNG string as `content` to a throwaway path — the committed file's size matched the *literal base64 text's character length*, not the decoded PNG's byte size, proving the tool re-encodes whatever string it's given as literal UTF-8 rather than decoding a supplied base64 payload back to binary. No lower-level git blob/tree-creation tool was found via `ToolSearch` to work around this. Cleaned up by deleting the throwaway test file from the branch. Consequence: the actual `fond-hero.jpg` binary is not yet in the repository — only the code referencing `/images/fond-hero.jpg` is pushed, so the header will 404/show broken until the JPEG itself is added. Delivered the optimized JPEG to Brett directly via file transfer in the chat and asked him to upload it himself to `public/images/fond-hero.jpg` on the `feature/kill-yoco-staff-tablet` branch via GitHub's web "Add file → Upload files" UI, since this session's toolset cannot commit it. **This is a durable constraint for any future binary-asset work on this repo from this session's toolset — plan around it (deliver binaries to Brett for manual upload, or find/request a different tool) rather than re-attempting the same push path.**

Testing: local build/e2e verification re-run after each of the two pushes above (CI-fix push, hero-image push), restoring `playwright.config.ts` to its clean committed form after each local run (a temporary `executablePath` override is needed locally in this sandbox due to a Playwright browser version mismatch, but was never committed). All checks green on PR #2 as of this entry.

Push: PR #2 open, CI green, not yet merged. Deployment: none yet. Live verification: none yet; pending the binary image upload (Brett, manual step) before the header can render correctly on the deployed build.

Open items carried forward: get `public/images/fond-hero.jpg` into the repo (blocked on Brett's manual upload, see above); once in, verify the header renders on the actual PR/CI build and update this file; merge PR #2 once reviewed; then deploy and do the usual live-verification pass; all open items from the pivot entry above remain open unchanged.

## 2026-09-15 — customer accounts, super-admin settings, 2FA and manual Yoco handoff

Implementation (branch `feature/complete-admin`, based on `origin/main` `99a2de4`): added named super-admin/admin/staff boundaries; super-admin-only write-only encrypted credential vault for Yoco checkout/webhook, Resend, Twilio and shared staff tablet code; named admin TOTP with one-use recovery codes; shared bootstrap admin code is disabled when an active named super admin enrolls 2FA. A one-time server `FOND_CREDENTIALS_KEY` remains necessary for encryption and must be backed up. Added `/account` sign-up/sign-in, user-owned order history, six-digit email verification and verified-account transactional emails through Resend, with status logs/retry. Staff tablet can alert while open, accept an order, enter the Yoco POS reference in an inline form and only then mark a new order ready. Pre-migration orders remain exempt. Added Twilio WhatsApp sender and approved Content SID settings; the previous Meta adapter remains a legacy fallback. Fixed failed-login rate limiting so successful sign-ins do not exhaust the threshold. `OPERATIONS.md` and `.env.example` now describe bootstrap versus dashboard configuration.

Testing: `npm.cmd run typecheck` passed; `npm.cmd test` passed 50/50, including encrypted vault, account ownership, email verification/one receipt, POS transition audit, staff-code rotation, Twilio ContentSid request, TOTP/recovery and failed-login threshold. `npm.cmd run build` passed with all new routes. The complete Playwright suite against that production build passed 32/32 on desktop and mobile with throwaway codes and an isolated in-memory database; an added super-admin/admin API-boundary test passed 2/2 separately after the full run. No real payment, email or WhatsApp provider transaction was made in these tests.

Commit/push: pending at time of this entry. Merge: pending. Deployment/configuration: not performed for this milestone yet. Live production verification: not performed; the existing production and separate `/opt/fond-test` staging clone still require promotion/configuration checks. Do not call this a live-complete release based on code tests alone.

Open gates: safely configure a unique backed-up vault key on each server without placing it in Git; provision actual named operators and save recovery codes; register and test a Yoco signed webhook and sandbox checkout on isolated staging before enabling online payment, followed by merchant-authorized live payment/refund reconciliation; verify Resend ownership/DNS for `fond.co.za`, set the sender/key and test a real verification and order email; confirm the bought Twilio number is a registered WhatsApp sender and the two Content templates are approved, then test opt-in delivery. Confirm restaurant staff can add one accepted FOND order to Yoco Counter and receive exactly one kitchen print, then record its Yoco reference in FOND. Automatic Yoco Counter order injection/printer routing is not implemented or provider-confirmed. Real-device browser notification, customer/restaurant UAT, approved menu/allergens/prices, offsite backup restore and operator training remain open. No Asana tracker is in scope.

### 2026-09-15 promotion and live smoke evidence

Implementation and local testing: as recorded above; this note records promotion evidence, not new functionality or provider UAT. Commit/push: `ca929e74154eedba589fced259ea73ecc2242f91` pushed to `origin/feature/complete-admin` and its branch Verify FOND run #74 passed. Merge: PR #6 merged into `main` as `9989d18da6ad1a5a0320c4b7a4776dcbfb47b33c`; main Verify FOND run #76 passed in 2m 3s. Deployment/configuration: Deploy FOND run #28 succeeded for that merge commit in 49s using the existing `/opt/fond` Compose/Traefik configuration and persistent volume. This run does not prove that the one-time vault encryption key or any provider credential has been configured. The separate `/opt/fond-test` staging clone has not been promoted to this commit or validated for this milestone.

Live production verification: after deploy run #28, `https://fond.mid-point.co.za/account` displayed the sign-in/create-account form and `https://fond.mid-point.co.za/admin` displayed its login form. `https://fond.mid-point.co.za/api/health` returned `status=ok`, `liveOrdering=true`, `payments=in-person`. These are anonymous smoke checks only; protected Settings, named roles, staff POS handoff, actual email, checkout, WhatsApp, kitchen printing and database migration effects have not been exercised on production. Keep the open gates above open. Online Yoco checkout remains off until sandbox and merchant validation. The provider dashboard requires a securely generated, backed-up `FOND_CREDENTIALS_KEY` on the server before secrets can be saved. No Asana tracker is in scope.

### 2026-09-15 operator-assisted vault and staging setup

Implementation/testing: no new application code. Operator transcript showed `FOND_CREDENTIALS_KEY` absent from both `/opt/fond/.env` and `/opt/fond-test/.env`; the operator generated distinct random keys, and a status-only readback showed both as valid 64-character hex values. Production Compose recreated `fond-fond-1`, and a status-only check inside the container confirmed the key was loaded. A refreshed, authenticated production Settings page no longer showed the vault-not-initialized warning and displayed super-admin provider controls; this session was signed in through the shared bootstrap code, not a named account. Brett confirmed both keys were backed up outside the VPS; their values were neither read nor recorded here.

Commit/push/merge: application promotion remains PR #6 and merge `9989d18` as above; this entry is documentation only. Deployment/configuration: staging data volume was confirmed different from production, then `/opt/fond-test` was fast-forwarded cleanly from `3e92393` to local production checkout `9989d18` while preserving untracked `compose.test.yaml` and `compose.traefik.yaml`. Staging Compose config validation and Docker build passed; `fond-test-fond-1` was recreated, its key was loaded, and `FOND_ALLOW_TEST_PAYMENTS=false` remained in staging configuration. A public health request immediately during replacement returned transient 404; a subsequent external readback returned HTTP 200. Live staging verification: `https://fond-test.mid-point.co.za/account` showed the new sign-in/create-account form, and `/admin` showed its login form. Protected staging Settings, actual accounts/orders, providers, Yoco POS print and payments are still untested. Production provider switches remain off; `/api/health` continues to report in-person payments. Operator provisioning, recovery codes and all provider/restaurant gates from above remain open. No Asana tracker is in scope.

### 2026-09-15 admin sign-in design correction

Implementation: the production screenshot showed the admin heading invisible because a global white `h1` style sat on the white shared staff login card; shared input styling also enlarged/centered the checkbox and stretched letter spacing in the access-code field. Replaced only the admin login markup with a FOND-branded, labelled card and scoped desktop/mobile CSS; preserved the existing bootstrap and named-account login behavior and button/field accessibility names.

Testing: local `npm.cmd run typecheck` and `npm.cmd run build` passed; focused Playwright management/role journeys passed 6/6 across desktop and mobile against a throwaway in-memory server. A local browser screenshot visually confirmed the new desktop hierarchy and legible form. No production credentials were used in these tests. Commit/push: `844f9eb` pushed on `feature/admin-login-polish`; PR #7 checks passed 2/2. Merge: PR #7 merged into `main` as `6fab09a`; main Verify FOND run #79 passed. Deployment/configuration: Deploy FOND run #29 passed for `6fab09a` using the existing production configuration. Live production verification: a fresh, unauthenticated Chrome session at `https://fond.mid-point.co.za/admin` showed the branded card, visible `Welcome back.` heading, separate named-account checkbox, labelled admin-code field and legible Unlock action; visual screenshot and accessibility tree agreed. This verifies the login screen only. Named super-admin enrollment/2FA and all previously open provider/restaurant gates remain open.

### 2026-09-15 reverse-proxy origin rejection during 2FA enrollment

Implementation: Brett's production Settings screenshot showed `Invalid origin.` immediately after Set up authenticator. The 2FA POST compared the browser Origin only with `request.url`, which can be the internal HTTP URL behind Traefik; customer email verification and provider credential POSTs had the same condition. A shared check now also accepts the exact origin of configured `FOND_PUBLIC_URL` or the HTTPS origin of the existing exact `FOND_HOST` route, and still rejects unrelated origins. No secret or OTP is handled differently. The hostname fallback was added after PR #8 deployment because production `FOND_PUBLIC_URL` was not independently confirmed; production `FOND_HOST` was confirmed earlier.

Testing: local typecheck and production build passed; 51/51 unit tests passed, including public-origin acceptance and hostile-origin rejection; the existing named-role/provider-settings Playwright journeys passed 2/2 on desktop/mobile using a throwaway in-memory server. After the hostname fallback, 51/51 unit tests and build passed again; a concurrent typecheck/build initially raced on generated `.next/types`, and sequential typecheck passed. Commit/push: `01828e3` pushed to `fix/proxy-origin-2fa`, with PR #8's two Verify checks passing; follow-up `9562f22` pushed directly to `main` with Verify FOND run #83 passing. Merge: PR #8 merged into `main` as `f3e319b`. Deployment/configuration: Deploy FOND run #30 passed for the merge and run #31 passed for final `9562f22`; the existing FOND_HOST production route remains unchanged. Live production verification: post-deploy health HTTP 200 returned `status=ok` and `payments=in-person`; the protected 2FA begin request still needs Brett to retry from his named session without disclosing the authenticator secret. 2FA activation, recovery-code storage and shared-admin-code cutoff are not verified on production. All prior provider/UAT gates remain open.

### 2026-09-15 QR authenticator enrollment

Implementation: the named-admin Settings 2FA enrollment now renders the existing `otpauth://` URI as a local browser canvas QR code; scanning with an authenticator app is the primary path. A collapsed, masked manual key supports setup on the same phone. The code-confirmation step still gates activation, and one-time recovery codes remain the final operator step. The URI is generated inside the browser with the `qrcode` dependency and is not sent to an external QR service.

Testing: `npm.cmd run build`, `npm.cmd run typecheck` and 51/51 unit tests passed. Focused desktop/mobile Playwright journeys passed 2/2 using a disposable URI/secret fixture, checking the rendered QR canvas, hidden manual key, reveal control and disabled/enabled six-digit confirmation. Commit/push: `19c3f80` pushed to `feature/qr-authenticator-setup`; PR #9's push and pull-request Verify checks passed, and main Verify FOND run #86 passed. Merge: PR #9 merged into `main` as `6e88dad`. Deployment/configuration: Deploy FOND run #32 succeeded for that merge; no provider switches changed. Live production verification: health HTTP 200 reported `status=ok` and `payments=in-person` after deployment. The live QR scan, authenticator confirmation and recovery-code backup for Brett's named account are still private operator UAT and are not proven by CI or the health response. All provider/order/restaurant gates remain open.

### 2026-09-15 staff sign-in design correction

Implementation: replaced the screenshot's cramped staff access card with the FOND-branded sign-in layout already used by the admin portal. The staff portal now has a visible heading, labelled access-code/password field, a full-width named-account selector and clearly separated action. Shared-code and named-account authentication behavior is unchanged. Updated the focused browser test for the new labels and to exercise both modes.

Testing: local `npm.cmd run typecheck`, `npm.cmd run build` and 51/51 unit tests passed. Focused Playwright staff sign-in and order-queue journeys passed 2/2 on desktop/mobile using a disposable local staff-code fixture; no production code was used. An initial test run failed because its old Unlock button locator no longer matched, and a second run without a configured local fixture failed at authentication; both test setup issues were corrected before the passing run.

Commit/push: `ba3e0f8` pushed to `origin/main`; Verify FOND run #34967574465 failed in the full Playwright step because two other staff journeys still selected the old Unlock button label, so the automatic deploy run #34967805301 was skipped. Those locators were then updated; their six desktop/mobile journeys and the entire 36/36 Playwright suite passed locally with isolated CI-style fixtures. Follow-up commit/push pending. Merge: direct main push, no PR. Deployment/configuration: pending; no provider credentials or payment switches changed. Live production verification: pending. Provider credentials, payment/email/WhatsApp UAT, Yoco POS/print confirmation and the earlier open gates remain outstanding.

Staff sign-in promotion follow-up (2026-09-15): implementation in `ba3e0f8`; corrected browser locators and handover in `744220d`, both pushed directly to `origin/main` with no PR merge. CI Verify FOND run #34968021219 passed for `744220d`; Deploy FOND run #34968184382 completed successfully on that exact head. No provider or server configuration changed. Live production verification: a fresh unauthenticated in-app browser session at `https://fond.mid-point.co.za/staff` showed the deployed FOND wordmark, STAFF PORTAL badge, legible `Ready for service.` heading, full-width named-account option, labelled staff-code field and `Open order queue` action; both the accessibility tree and visual screenshot confirmed this. Production staff authentication with Brett's private code and real staff/tablet order UAT were not performed. All earlier provider and restaurant gates remain open.

### 2026-09-15 published menu modifiers and required customer contact number

Implementation: inspected the production `/api/menu` before editing: 144 available items and zero modifier options. Added a one-time, idempotent modifier migration for existing SQLite menus, based on the already-transcribed FOND menu: published vegan swaps, R0 ingredient removals, smoothie boosters, coffee/milk/juice add-ons, gluten-free bread/bagel/base upgrades and their stated menu prices. The migration fills only empty modifier lists on products whose description still matches the seed; existing admin-configured options and changed product descriptions remain untouched, and later deliberate clearing is preserved. The customer menu already had modifier selection/pricing; the staff manual-order screen now also selects variants and uses quoted totals. Customer collection and delivery now both show a required contact-number field, and `createOrder` rejects missing/invalid contact numbers on customer orders even when the browser is bypassed. Follow-up after initial deployment: staff-created walk-in/table orders now also require a contact number in the manual form and server API; existing orders remain readable.

Testing: `npm.cmd run typecheck` and `npm.cmd run build` passed; 53/53 unit tests passed, including existing-menu migration, preservation of admin options/descriptions and required customer phone. The complete 38/38 desktop/mobile Playwright suite passed with disposable CI-style local fixtures. Browser journeys verified customer add-on selection, staff modifier payload and contact-number rejection through the form and API. No production customer order or kitchen print was created during validation.

Commit/push: pending. Merge: pending. Deployment/configuration: pending; this migration will change eligible published menu items on the existing production database when the new code serves its first menu request. Live production verification: pending. Restaurant confirmation of every offered ingredient removal/add-on, menu prices, stock, allergens and Yoco POS mapping remains an operational gate. Provider payment/email/WhatsApp and real-device UAT gates remain open.

Menu modifier/contact promotion follow-up (2026-09-15): implementation, tests and context committed/pushed directly to `origin/main` as `19f51a3` (no PR merge). Verify FOND run #34969782700 passed on that head; Deploy FOND run #34969993066 completed successfully on the same head with the existing Compose/Traefik configuration and SQLite volume. No provider credentials or payment switches changed. Live production verification: after deploy, `https://fond.mid-point.co.za/api/menu` returned 144 available items, 65 with modifiers and 166 options; Tropical Gold exposed three R30 smoothie boosters and Smashed Avo exposed a R0 vegan swap and R0 ingredient removals. A fresh unauthenticated in-app browser session at `https://fond.mid-point.co.za/` displayed selectable options on breakfast cards and a labelled contact-number field in both collection and delivery checkout. Production submission without a number was not attempted; server rejection was verified locally. The existing restaurant approval, stock/allergen, POS mapping, payment/email/WhatsApp, real-device and training gates remain open.

Staff contact-number correction (2026-09-15): after the menu/options deployment, confirmed that the initial required-number change still exempted staff-created manual orders. The follow-up makes a contact number mandatory for every newly created order, including walk-in/table orders entered by staff; the staff form and `/api/staff/orders` now carry it to the same `createOrder` validation. Existing order rows are unchanged. Testing: local typecheck, production build, 53/53 unit tests and full 38/38 desktop/mobile Playwright journeys passed using disposable fixtures; the unit suite now explicitly rejects staff orders with no number and manual-order browser tests fill the required field. Commit/push, deployment and live verification for this follow-up are pending. The prior menu/options CI and live evidence above remain valid for that deployed commit; the new staff requirement is not yet live.

Staff phone promotion follow-up (2026-09-15): implementation, tests and handover committed/pushed directly to `origin/main` as `15d1e1a` (no PR merge). Verify FOND run #34970455928 passed on that head; Deploy FOND run #34970640217 completed successfully on the same head with the existing Compose/Traefik configuration. Live production verification: after refreshing Brett's existing authenticated Edge staff tab at `https://fond.mid-point.co.za/staff`, the Add order drawer displayed selectable breakfast modifiers and a labelled Contact number field. The drawer was closed without submitting or changing the live order queue. A real staff submission with an omitted or valid private phone number was not attempted in production; local unit/browser checks proved the server behavior. The existing new customer phone requirement and 65-item/166-option live menu evidence remain valid. Restaurant menu/stock/allergen/POS mapping and provider/real-device/training gates remain open.

### 2026-09-15 six-stage staff queue, timing and sound

Implementation: replaced the three-column staff queue with New, Awaiting Yoco payment confirmation, Added to Yoco system, Preparing, Ready for delivery and Ready for collection. Payment and POS lanes are derived from the stored checkout/payment records and manually confirmed Yoco POS entry; they do not pretend that FOND creates a Yoco POS order or prints automatically. Added an audited `preparing` order status and stage-specific actions; accepted orders require POS confirmation before preparation, pending Yoco checkout cannot be accepted before a matching signed Yoco payment, and ready orders split by requested fulfilment with Mark delivered/collected actions. Added per-stage elapsed/target timing, a red pulsing Delayed badge (static under reduced-motion preference), coloured lane indicators, explicit horizontal navigation and a user-enabled WebAudio sound for newly received orders while the tablet page is open. Preparation target reads the existing store setting; other targets are fixed initial estimates (5/10/5/10/10 minutes), subject to restaurant calibration.

Testing: local `npm.cmd run typecheck`, production `npm.cmd run build`, 57/57 unit tests and 38/38 desktop/mobile Playwright tests passed using disposable codes and an in-memory database. Added tests for all six routing lanes, stage timing, pending-checkout acceptance guard, audited preparation transition and the visible tablet workflow; targeted staff browser retest passed 2/2 after final assertions. These checks use simulated provider records and no real customer payment, Yoco POS insertion, kitchen printer or live sound device.

Commit/push: pending at this entry. Merge: direct-main promotion planned, no PR merge. Deployment/configuration: pending; no provider credential or setting change is part of this release. Live production verification: pending. Operational gates: staff must still enter the order in the actual Yoco POS, record its real reference, and confirm printer output; payment webhook/provider reconciliation, realistic delay thresholds, tablet sound/notification permissions, real customer and delivery UAT, and the previously recorded provider/menu/stock/training gates remain open.

Queue promotion follow-up (2026-09-15): implementation, tests and initial handover committed/pushed directly to `origin/main` as `48e174d` (no PR merge). Verify FOND run #34971604748 completed successfully on that head, including the browser and Docker checks; Deploy FOND run #34971822821 completed successfully on the same head with existing configuration. Live production verification: refreshed Brett's existing authenticated Edge tab at `https://fond.mid-point.co.za/staff` after deployment. The page showed six distinct coloured lanes, sound control, left/right navigation, elapsed stage target and a red pulsing Delayed badge on the existing 35-minute-old New order. Screenshot and accessibility tree confirmed this. No production order was advanced, cancelled or submitted; real Yoco payment/checkout, POS entry/printing, audible sound on a newly incoming order, delivery/collection fulfilment, realistic timing calibration and staff-device UAT remain unverified. The provider/menu/stock/training gates above remain open.

### 2026-09-16 configurable queue timing targets

Implementation: added six Settings controls for New, Yoco payment confirmation, Yoco system entry, Preparing, Ready for delivery and Ready for collection targets. Each accepts 1–240 minutes and drives the tablet's elapsed/Delayed calculation through the public store settings. Stored settings from the earlier schema inherit safe defaults instead of returning missing fields. Fixed kitchen-capacity counting so orders in the new `preparing` status remain active. Browser sound still requires a deliberate user click after page load due to browser autoplay rules.

Testing: local typecheck passed; 59/59 unit tests passed, including legacy-setting migration, invalid timing, Preparing capacity and custom lane timing; the production build passed; 38/38 desktop/mobile Playwright journeys passed. The management browser journey changed the New target to seven minutes, saved through the dashboard and verified it through `/api/store`. Provider calls and production order mutations were not made.

Commit/push: pending at this entry. Merge: direct-main promotion planned, no PR merge. Deployment/configuration: pending; production values will initially inherit 5/10/5/20/10/10 minute defaults until an admin saves chosen targets. Live production verification: pending. Restaurant calibration and the previously recorded provider, POS/printing, sound-device, fulfilment and training gates remain open.

Timing-target promotion follow-up (2026-09-16): implementation, tests and initial handover committed/pushed directly to `origin/main` as `82fe068` (no PR merge). Verify FOND run #35055523265 passed on that head and Deploy FOND run #35055661334 completed successfully on the same head with existing configuration. Live production verification: `https://fond.mid-point.co.za/api/health` returned healthy ordering with payments still in-person; `/api/store` returned all six target values as 5/10/5/20/10/10 minutes, proving the deployed runtime and backward-compatible production settings. A fresh protected Settings-page visual inspection was not completed because the existing Edge tab could not be attached during this verification; local desktop/mobile browser tests proved the controls and save/readback behavior. No production setting or order was changed. Restaurant timing calibration and all previously recorded provider, POS/printing, sound-device, fulfilment and training gates remain open.

### 2026-09-16 item preparation estimates and isolated Yoco sandbox checkout

Implementation: added a preparation estimate to every menu record and exposed it in each admin menu-item editor. Existing published items receive a one-time research-derived starting estimate; later admin changes are preserved. Initial category ranges use published restaurant ticket-time guidance and dish-specific overrides for slower/faster cooking methods. Customer menu cards show the estimate with a configurable uncertainty buffer, initially 7% and constrained to an editable 5–8%; new orders snapshot both item and weighted basket estimates so later menu edits do not rewrite order history. The staff Preparing lane now uses the basket estimate. This remains an operating estimate: FOND should replace starting values with measured service data by item/station.

Yoco implementation: added a super-admin-only sandbox checkout action and provider checklist. Test credentials and signed test webhook processing are explicitly isolated from the customer checkout endpoint: a test key never makes the public Pay with Yoco action available, and normal checkout requires live credentials plus the public online-checkout switch. The sandbox action creates a clearly labelled do-not-prepare order from the cheapest available item meeting Yoco's minimum amount, then uses the same trusted server-side total, hosted redirect, idempotency and signed-webhook reconciliation path. Health reports `yoco-sandbox` separately when test configuration is complete. No provider secret is stored in Git or returned by the settings API.

Testing: `npm test` passed 61/61; `npm run build` passed, including TypeScript and all routes; the complete Playwright suite passed 38/38 on desktop/mobile with throwaway admin/staff codes and an in-memory database. Focused final browser checks passed 14/14 for management/ordering after adding authorization and visible-estimate assertions, then 2/2 for the corrected case-sensitive estimate locator. `git diff --check` passed. The repository has no lint script. Tests cover one-time estimate seeding and edit preservation, the 5–8% bounds, slowest-item basket calculation, sandbox-only checkout reuse, anonymous sandbox rejection and customer-visible estimated time. These are mocked provider checks; no real Yoco sandbox card or webhook was exercised.

Commit/push: pending at this entry. Merge: direct-main promotion planned, no PR merge. Deployment/configuration: pending; saving the Yoco test secret and webhook signing secret, registering the exact webhook URL and enabling sandbox processing remain operator steps in the super-admin dashboard. Live production verification: pending. Open gates: complete one actual hosted Yoco test-card checkout and prove the signed event reconciles the matching Finance record exactly once; then disable sandbox processing before live credentials/public checkout are enabled. Merchant-authorized live payment/refund, Yoco POS/manual entry and kitchen printer confirmation, measured preparation-time calibration, staff/customer UAT and all earlier provider/menu/stock/training gates remain open. No Asana tracker is in scope.

Preparation/Yoco promotion follow-up (2026-09-16): implementation, tests and the initial handover were committed and pushed directly to `origin/main` as `97e1dc6` (no PR merge). Verify FOND run #35057342052 passed on that exact head, and Deploy FOND run #35057460769 completed successfully with the existing production Compose/Traefik configuration and SQLite volume. No provider credential or payment switch was changed. Live production verification after deployment: `/api/health` returned `status=ok` and `payments=in-person`; `/api/store` returned preparation weighting 7%, sandbox processing false and public online payment false; `/api/menu` returned 144/144 items with valid estimates, including espresso 3, 350g rump 20 and Classic Margherita Pizza 15 minutes. This proves deployment, migration and anonymous readback, but not a protected admin edit or an actual Yoco transaction. The next operator step is to save the test secret and webhook signing secret in the super-admin dashboard, register its displayed webhook URL in Yoco, enable sandbox processing only, and complete one signed hosted test checkout. All open gates above remain open until that evidence exists.

Public URL correction (2026-09-16): live protected Settings inspection after the above deployment found the Yoco checklist displayed only `/api/payments/webhook`, proving `FOND_PUBLIC_URL` was absent or invalid in the running container even though the confirmed Traefik `FOND_HOST` route was available. Added a validated HTTPS public-base helper: a valid explicit public URL remains first choice, otherwise the exact DNS-style `FOND_HOST` is used; malformed host/path input fails closed. Checkout configuration, redirect construction and the displayed webhook URL now share this helper. Local 62/62 unit tests and production build passed, including explicit-URL, malformed-markdown fallback and hostile-host rejection. Commit/push, Verify, deployment and live full-URL readback are pending. No credential or switch was changed, and no Yoco transaction was created.

Public URL follow-up: commit `bed7b12` passed Verify FOND run #35057864832 and Deploy FOND run #35057991489, but protected live readback showed `Public URL not configured`; production Compose uses the hostname for Traefik label interpolation without injecting either URL variable into the container. Because FOND is a single-tenant Midpoint service, the helper now has the verified canonical production origin `https://fond.mid-point.co.za` as its final fallback. Explicit valid configuration and a valid routed host still take precedence. Final commit, CI/deployment and protected full-URL readback are pending.

### 2026-09-16 independent Yoco sandbox credential validation

Implementation: protected live Settings readback confirmed Brett's `sk_test_…` value was encrypted and stored, while the separate webhook subscription secret remained absent. The admin sandbox checkout was incorrectly gated by the complete production-payment readiness check, creating a circular dependency: it required `whsec_…` before it could validate the Checkout API key. Sandbox checkout creation now requires the stored test secret, the explicit sandbox-processing switch and a secure public URL; public customer checkout still requires live credentials, the public switch and the webhook secret. The dashboard checklist now states that `whsec_…` is returned once when the webhook subscription is created.

Testing: 62/62 unit tests passed, including sandbox checkout with no webhook secret and fail-closed public checkout; sequential typecheck and production build passed. Live pre-fix readback confirmed the test key showed `Stored`, sandbox processing saved successfully, and the deployed test action returned `Online payment is not available`, reproducing the over-gating. Commit/push, CI, deployment and post-deploy real Yoco sandbox checkout remain pending. No key was exposed or added to source control. Public online checkout remains disabled. The webhook subscription and one signed Finance reconciliation remain required before payment integration is accepted.

Promotion/live follow-up: implementation, tests and initial handover were committed and pushed directly to `origin/main` as `268eb83` (no PR merge). Verify FOND run #35060250405 passed and Deploy FOND run #35060386751 completed successfully on that exact head with the existing production configuration. Protected live Settings readback showed the test key stored, sandbox processing enabled, public online checkout disabled and the revised four-step checklist. The live admin action successfully authenticated to Yoco and created sandbox checkout `ch_NDLzAv6M3jAPTjnub2vC2axa` for isolated reference `FOND-BAB5316B007B4BF481E965F8CBD5F6A7`, amount R10.00, returning a valid hosted `c.yoco.com` link. The test payment itself has not been completed. Webhook subscription creation, its one-time signing secret, signed-event Finance reconciliation and disabling sandbox processing afterward remain open gates.

Sandbox-orphan correction (2026-09-16): live staff readback exposed an earlier failed test attempt (`FOND-66B442C69AE24A62886D1FB61F8DC96A`) in New with a misleading manual Yoco POS-entry form, while the successful checkout correctly remained in Awaiting Yoco payment confirmation. The admin test route now cancels its newly created do-not-prepare test order if checkout creation fails, preventing future provider/configuration errors from leaving active POS work. The valid sandbox payment returned to FOND, but still awaits its signed webhook. Local 62/62 unit tests, typecheck and production build passed. Commit/push, CI/deployment and cleanup of the existing orphan are pending; cancelling that existing test record is a separate live order mutation. Public online checkout remains disabled.

### 2026-09-16 server-side Yoco Checkout webhook registration

Implementation: confirmed against Yoco's Checkout API reference that FOND's existing signed-payment handler matches Checkout webhooks and that these are registered directly with the stored `sk_test_…` or `sk_live_…` key at `POST https://payments.yoco.com/api/webhooks`. The separate Developer Console application is not required for this Checkout integration. Added a super-admin-only Settings action that registers the exact FOND callback URL from the server, validates Yoco's returned ID, mode, URL and one-time `whsec_…` value, then saves that signing secret directly into the encrypted provider vault. The key and returned secret are never sent to browser code. Registration is blocked when a webhook secret is already stored, and sandbox checkout stays disabled until registration succeeds.

Testing: local `npm test` passed 64/64, including request URL/header/body validation, successful encrypted one-time-secret storage and rejection of malformed provider responses. `npm run typecheck` and the production `npm run build` passed; the build includes `/api/admin/yoco/register-webhook`. These tests mock the provider response and do not prove a live subscription or signed delivery. Commit/push: pending. Merge: direct-main promotion planned, no PR merge. Deployment/configuration: pending. Live verification: pending registration in protected production Settings, followed by a new R10 hosted sandbox payment and exact signed Finance reconciliation. The existing completed test checkout predates webhook registration and is not proof of callback delivery. Public checkout remains disabled. The unnecessary Developer Console app/API key should be revoked after explicit operator approval; no credential value is recorded here. No Asana tracker is in scope.

Promotion and registration follow-up: committed and pushed directly to `origin/main` as `5511bb2` (no PR merge). Verify FOND run #100 (`35062244066`) passed in 1m 56s, and Deploy FOND run #46 (`35062383432`) completed successfully in 42s using the existing production Compose/Traefik configuration and SQLite volume. Live route readback changed from HTTP 404 before deployment to HTTP 405 for an unsupported GET after deployment, proving the new protected registration route was served. In Brett's authenticated named super-admin session, Settings showed the stored test key, missing webhook secret and exact secure callback URL. After Brett explicitly approved the provider subscription, the live `Register Yoco webhook securely` action succeeded. The dashboard then showed `Yoco webhook signing secret · Stored`, disabled the registration action as `Yoco webhook registered`, enabled `Create Yoco sandbox checkout`, and displayed the success notice that the Yoco test webhook and signing secret were saved securely. This proves subscription creation and local secret storage, but not event delivery or reconciliation. A fresh R10 hosted sandbox payment, signed callback, Finance readback and subsequent sandbox shutoff remain required. Public online checkout remains disabled. The separate Developer Console application/API key is still present pending explicit deletion approval. No Asana tracker is in scope.

Signed sandbox payment follow-up: the live protected action created fresh checkout `ch_QLOBVJy4PAJhbBBfvpkIy1kx` for isolated do-not-prepare order `FOND-27B2BCFE029D4822A5846220DD6DD658`, amount R10.00. Yoco displayed Test mode and stated that no funds would be processed. Brett completed the hosted test transaction and Yoco returned to the exact FOND success URL. After refreshing Finance, the order showed value R10.00, paid R10.00 and balance R0.00; Online checkout reconciliation showed that checkout as `paid`; and the ledger recorded one R10.00 Yoco receipt `yoco:p_QLOBVJy4jjBhX8ZHyONHY4Rx` at 2026-09-16 08:24 SAST. The live staff queue showed the exact reference in New with a visible `Paid` state and `Accept order`, while the earlier pre-webhook checkout remained separately pending. This is live evidence that the signed Checkout webhook was accepted, matched to the stored checkout and reconciled exactly once. The test order was deliberately not accepted, entered into POS or prepared. Public online checkout remains disabled. Remaining provider gates are sandbox shutoff, cleanup of isolated test orders, revocation of the unnecessary Developer Console application/API key, then later live-key merchant payment/refund and actual Yoco POS/printer UAT. No Asana tracker is in scope.

Sandbox shutoff follow-up: immediately after the successful signed-payment proof, the protected production Settings switch `Allow Yoco sandbox webhook processing` was turned off and saved. Anonymous live readback confirmed `/api/store` has `allowTestPayments=false` and `onlinePaymentsEnabled=false`; `/api/health` reports healthy ordering with `payments=in-person`. The encrypted test key and webhook secret remain stored for controlled future testing, but customer online checkout is unavailable and new test events fail closed until a super admin explicitly re-enables sandbox processing. Cleanup of the paid and older pending isolated test orders, deletion of the unnecessary Developer Console application/API key, live-key payment/refund and actual Yoco POS/printer UAT remain open.

Sandbox cleanup follow-up: after Brett explicitly approved destructive cleanup, the paid isolated order `FOND-27B2BCFE029D4822A5846220DD6DD658` and the older pre-webhook pending order `FOND-BAB5316B007B4BF481E965F8CBD5F6A7` were cancelled from the protected live staff queue. Full queue readback then showed New=2 with only the two existing John orders and Awaiting Yoco payment confirmation=0; no sandbox card remained active. The historical payment/checkout/audit records remain available in Finance. The unnecessary live personal Yoco Developer Console application `FOND Midpoint Webhooks` was then deleted by exact-name confirmation; Yoco warned that deletion permanently revokes its API keys, and final Applications readback stated there are no live applications. This does not delete or disable the separately registered Checkout API webhook, whose signing secret remains encrypted in FOND. Current production payment posture remains sandbox processing off and public online checkout off. Remaining Yoco gates are live-key payment/refund and real restaurant POS/kitchen-printer UAT.

### 2026-09-16 customer payment policy and end-to-end ordering flow

Implementation: customer checkout now records an explicit payment choice. Collection customers can choose secure Yoco payment or payment at FOND; delivery requires Yoco online payment before the order can be prepared. The public store API exposes checkout availability and sandbox/live mode without returning credentials. A Pay now order creates the trusted server-side order and hosted Yoco checkout together and redirects the customer to Yoco. Online-payment orders remain in Awaiting Yoco payment confirmation before checkout creation and after any provider interruption, and staff cannot accept them until signed payments cover the trusted order total. Pay-at-collection orders remain operationally available and show the amount due. Customer tracking and staff cards now show the chosen payment route, paid/pending/due state, fulfilment and preparation estimate. Test checkout exposure fails closed outside the exact `fond-test.mid-point.co.za` staging hostname; production cannot expose the stored test key as customer checkout. Added a styled payment-choice panel and made Playwright's disposable access-code fixtures self-contained.

Testing: local `npm test` passed 66/66, including delivery policy, pre-checkout acceptance blocking and payment-lane routing. `npm run typecheck` and the production `npm run build` passed. The complete Playwright suite passed 38/38 across desktop and mobile, including collection ordering, the unavailable-online-payment safeguard, account history, customer tracking, staff queue/POS transitions, admin roles, reliability and access boundaries. The first browser run had 20 fixture failures because local admin/staff environment variables were absent; no application assertion failed, and the configuration was corrected before the clean run. No provider transaction was made in these local checks.

Commit/push: customer payment implementation and its initial handover were committed and pushed directly to `origin/main` as `c31d77d` (no PR merge). Verify FOND run #`35067891605` passed on the exact head, and Deploy FOND run #`35068063912` completed successfully with the existing production Compose/Traefik configuration. Anonymous production readback returned healthy ordering, `payments=in-person`, `onlinePayments=false`, `paymentMode=none`, sandbox processing off and public online checkout off; collection remains available. This proves the safe production posture and served API, not a customer payment. A follow-up added verified-main automatic deployment to the already isolated `/opt/fond-test` checkout using its existing staging Compose overlays. Its first run reported success but public readback still returned the old API shape. Investigation found that `/opt/fond-test` retained the local-clone origin `/opt/fond`, and the parallel staging job exited before production advanced that local remote. The corrected job waits for production and resets staging's origin to the canonical GitHub repository before verifying the exact SHA. Corrected staging promotion evidence is pending. Staging must then have its secure `https://fond-test.mid-point.co.za` public URL, stored test key and Checkout webhook secret, and sandbox processing enabled. Live verification remains pending a staging customer journey for both collection paths and delivery, one hosted R10 test payment, signed webhook reconciliation, staff acceptance/POS-reference/preparing/ready/complete transitions, and customer tracking/account readback. Actual Yoco POS entry and kitchen printing are still manual and require restaurant UAT. Live-key payment/refund, delivery operations, real-device notification/sound, email/WhatsApp, timing calibration, menu/stock/allergen approval and staff training remain open. No Asana tracker is in scope.

### 2026-09-16 staging Yoco payment reconciliation and staff 2FA correction

Implementation: the isolated staging customer journey created collection order `FOND-3B335B0D65FB43BFBEBC44A69DA94050` for R95.00 using the explicit Yoco online option and hosted checkout `ch_ax4bzJp0JbRsydAhe5WujvEj`. The completed test payment returned to the exact staging order-tracking URL. Finance readback showed order value R95.00, paid R95.00, balance R0.00, checkout status `paid` and exactly one Yoco ledger receipt. Customer tracking showed `Paid online` and waiting for FOND acceptance. During staff continuation, the named super-admin account with active 2FA could not sign in because the staff form did not collect an authenticator code and the staff login route did not pass one to `loginMember`. The staff portal now includes an authenticator/recovery-code field for named accounts and submits it through the API; shared staff-code login remains unchanged.

Testing: after the correction, local `npm test` passed 66/66, `npm run typecheck` passed, the production `npm run build` passed, and the complete Playwright suite passed 38/38 across desktop and mobile. Browser coverage checks that the 2FA field appears only in named-account mode. Existing unit coverage proves an enrolled named super-admin is rejected without 2FA and accepted with a valid authenticator or unused recovery code. The live paid staging order has not yet been advanced through staff stages.

Commit/push: pending at this entry. Merge: direct-main promotion planned, no PR merge. Deployment/configuration: pending automatic production and isolated staging deployment; no provider credential or payment switch changes are part of this correction. Live verification after deployment must sign into staging staff with Brett's private password/current OTP and move the paid test order through acceptance, a clearly marked staging-only manual POS reference, preparation, ready for collection and collected. This will prove the application lifecycle only. Actual entry in the restaurant Yoco POS and kitchen-printer output, live-key payment/refund, production checkout enablement, delivery operations, customer email/WhatsApp, real-device notification/sound, timing calibration, menu/stock/allergen approval and staff training remain open gates. No Asana tracker is in scope.

Staff 2FA promotion and staging lifecycle follow-up (2026-09-16): implementation, tests and the initial evidence entry were committed and pushed directly to `origin/main` as `b8b039f` (no PR merge). Verify FOND run #`35075213983` passed on that exact head. Deploy FOND run #`35075394694` completed successfully with both the production `deploy` job and isolated `deploy-staging` job green; no provider credential or payment switch changed. Post-deploy staging verification showed the named-account staff form includes the authenticator/recovery-code field. Brett signed in with the existing staging super-admin account and private OTP. The paid R95.00 order `FOND-3B335B0D65FB43BFBEBC44A69DA94050` was accepted, assigned the explicit non-provider test reference `STAGING-TEST-NO-POS-ch_ax4bzJp0`, moved into Preparing with its 13-minute basket estimate, marked Ready for collection, and marked Collected. Final staff readback showed all six active lanes empty. Customer tracking for the exact reference showed `Collected`, R95.00, approximately 13 minutes and `Paid online`. Combined with the earlier Finance readback of one R95.00 Yoco receipt and R0.00 balance, this proves the isolated application flow from customer order through hosted sandbox payment, signed webhook reconciliation, staff processing and customer status completion. The recorded POS reference was deliberately simulated; no order was inserted into the restaurant's actual Yoco POS and no kitchen printer output occurred. Those restaurant UAT gates, plus live-key payment/refund, production checkout enablement, delivery operations, email/WhatsApp, real-device alerts, timing calibration, menu/stock/allergen approval and staff training, remain open. No Asana tracker is in scope.

### 2026-09-16 prominent staff payment-state banners

Implementation: staff order cards now surface payment state as a high-contrast banner near the top of every card. Signed payment covering the trusted order total renders green `PAID ONLINE` with a check icon; an online-payment order without confirmed funds renders amber `PAYMENT PENDING · DO NOT PREPARE`; a pay-at-FOND order renders red `PAYMENT DUE AT FOND` with the outstanding amount. The green state is derived from the reconciled payment ledger, not the customer's return from Yoco. Existing server transition guards still prevent staff accepting an online-payment order before signed reconciliation.

Testing: local `npm test` passed 66/66, `npm run typecheck` passed, the production build passed, and the complete Playwright suite passed 38/38 across desktop and mobile. The staff POS journey now asserts the prominent pay-at-FOND banner before acceptance. Commit/push: pending at this entry. Merge: direct-main promotion planned, no PR merge. Deployment/configuration and live verification: pending. No payment/provider setting changes are included. A future active paid staging order should be visually checked for the green banner after deployment; the earlier paid staging order is already completed and therefore absent from the active queue. Actual restaurant Yoco POS and printer UAT remains open.

Payment-banner promotion follow-up (2026-09-16): implementation, tests and initial handover were committed and pushed directly to `origin/main` as `efd460a` (no PR merge). Verify FOND run #`35076757876` passed on that exact head. Deploy FOND run #`35076951921` completed successfully with both production and isolated staging jobs green; no provider credentials or payment settings changed. The earlier completed paid order is not present in the active queue, so live green-banner inspection awaits the next active paid staging order. Local desktop/mobile browser coverage proves the card banner layout and the pay-at-FOND state; the signed-ledger condition for green remains covered by payment/order tests and the server guard. Actual Yoco POS/printer UAT remains open.

### 2026-09-16 staff fulfilment details and dispute audit trail

Implementation: staff cards now state COLLECTION or DELIVERY explicitly. Collection cards show who is collecting, contact number, requested time and order age; delivery cards show recipient, contact number, building, company, requested time and age. Payment copy now says `PAID ONLINE` with amount, `ONLINE PAYMENT NOT COMPLETED · DO NOT ACCEPT OR PREPARE`, or `PAY IN PERSON ON COLLECTION/DELIVERY` with amount due. Added an authenticated per-order audit drawer that reads the durable order events, Yoco checkout ID/status/timestamp and payment-ledger receipts/references. Cancelling or completing an order removes it from the active tablet lanes but retains it in admin order history, Finance and the underlying audit tables for dispute review.

Testing: 66/66 unit tests passed; typecheck and production build passed. Focused staff desktop/mobile Playwright passed 2/2 and verifies collection identity/contact details plus the permanent audit record. A complete isolated-database browser run passed 37/38; the sole unrelated promotions test hit a transient local `ECONNRESET` while intercepting `/api/store`, then passed 2/2 on immediate isolated rerun. No application assertion related to fulfilment, payments or audit failed. Commit/push, deployment and live staging verification are pending at this entry. Existing test orders remain staging-only and actual restaurant POS/printer UAT remains open.

Fulfilment/audit promotion follow-up (2026-09-16): implementation, tests and initial handover were committed and pushed directly to `origin/main` as `1f438b8` (no PR merge). Verify FOND run #`35080182356` passed on that exact head. Deploy FOND run #`35080385318` completed successfully with both production and isolated staging jobs green; no provider setting changed. Live staging readback showed the three test cards with explicit COLLECTION, collecting person, phone and requested time; the payment banners read `PAY IN PERSON ON COLLECTION · R32.00 DUE`, `ONLINE PAYMENT NOT COMPLETED · DO NOT ACCEPT OR PREPARE`, and `PAID ONLINE · R32.00`. Opening the paid card's audit trail showed checkout `ch_DLzAv6qPB9AUpV4sm7uj6Pxa` as paid, one R32.00 Yoco receipt, and the created, accepted, POS-recorded, preparing and ready events with actors/timestamps. This proves the dispute trail and live card details on staging. Actual restaurant POS/printer UAT remains open.

### 2026-09-16 tablet operations and in-person payment completion gate

Implementation: the staff server now refuses to complete a ready order until the payment ledger covers the full trusted order total. Ready pay-at-collection orders expose a dedicated `Take payment` action for cash or card; card requires a terminal receipt reference, cash receives a unique system reference, and both retain staff actor/time evidence in the permanent payment audit. The card then identifies `PAID IN PERSON · CASH/CARD`, while signed Yoco receipts remain `PAID ONLINE`. `Mark collected` or `Mark delivered` appears only after full payment. The tablet interface now has a service header with live refresh state, four queue summary counters, three readable columns at normal tablet width, larger touch actions, less cramped references, and focused POS/payment dialogs instead of forms expanding inside order cards. Phone-width horizontal snap was removed after it caused touch targets to shift during interaction.

Testing: `npm.cmd run typecheck`, 66/66 unit tests and the production build passed. Unit coverage proves an unpaid ready order cannot complete. Focused staff browser coverage passed on the touch/mobile viewport and completes accept -> POS record -> prepare -> ready -> cash payment -> collected. The complete isolated-database Playwright suite passed 38/38 across desktop and mobile after analytics/reliability fixtures were updated to use the same payment gate. `git diff --check` passed. These checks use local cash/POS fixtures; no real restaurant terminal or printer was contacted.

Commit/push, merge, deployment/configuration and live staging verification are pending at this entry. Direct-main promotion is planned with no PR merge and no provider setting change. Live staging should exercise one existing pay-in-person test order through manual POS entry, preparation, ready, cash/card recording and completion, proving the payment banner and audit record. Remaining operational gates include actual restaurant Yoco POS/printer output, live-key payment/refund, failed/abandoned-checkout exception handling, delivery assignment/proof, customer email/WhatsApp, durable device alerts, stock/allergen operating controls, named staff onboarding, measured timing calibration, outage procedure and staff training. No Asana tracker is in scope.

Tablet/payment promotion follow-up (2026-09-16): implementation, tests and the initial evidence entry were committed and pushed directly to `origin/main` as `d07a71a` (no PR merge). Verify FOND run #`35081744944` passed on that exact head and Deploy FOND run #`35081932818` completed successfully with both production and isolated staging jobs green; no provider credential or payment switch changed. Direct staging asset readback contained the new `Live order board`/`Take payment` release and `/api/health` remained healthy in `yoco-sandbox` mode. A cache-busted authenticated staging tablet inspection then showed the new service header/live indicator, last-refresh timestamp, active/attention/ready/payment counters, three readable columns, explicit payment and fulfilment blocks, and compact reference placement. Opening `Record Yoco entry` showed the focused professional POS handoff dialog with its receipt/order-number field and clear manual-entry explanation; it was closed without submitting or changing the staging order. Local desktop/mobile browser coverage proves the new ready-order cash-payment and completion path, but a live staging cash/card record was not created. Actual restaurant POS/printer UAT and all operational gates listed above remain open. The canonical `PROJECT_CONTEXT.md` is on `origin/main`; no Asana tracker is in scope.

### 2026-09-16 final Yoco POS operating decision

Business decision: Brett approved the existing manual handoff as FOND's final operating model. The supported flow is `FOND customer order -> signed Yoco online-payment confirmation or pay-in-person status -> staff accept -> staff capture the products/modifiers in the restaurant Yoco POS -> Yoco POS sends the order slip to its configured kitchen printer -> staff record the Yoco receipt/order reference in FOND -> preparation and fulfilment continue in the FOND tablet queue`. FOND will not wait for or claim an automatic public API connection that creates restaurant orders in Yoco POS.

Status: the architecture/product decision and required FOND software workflow are complete. The deployed staff queue already enforces the human-confirmed POS-reference gate before preparation and retains the actor, timestamp and reference in the audit trail. Automatic Yoco POS order creation is removed from the development backlog and must not be described as an outstanding defect. The restaurant still needs a one-time launch walkthrough proving staff can capture a representative order with modifiers and that the already configured Yoco kitchen printer produces the correct slip; this is operational acceptance/training for the chosen manual process, not an integration-development dependency. No code, provider credential or payment configuration changed for this decision. No additional test or deployment was required. No Asana tracker is in scope.

### 2026-09-16 controlled Twilio WhatsApp test facility

Implementation: added a super-admin-only WhatsApp test action in Provider credentials. It sends either the approved order-accepted or order-ready Content SID to one explicitly entered consenting recipient without creating a fake order. The server validates same-origin requests, normalizes the recipient to international format, requires the complete encrypted Twilio configuration, uses the existing server-side Twilio adapter, and records only the template type in the admin audit; the test phone number and provider secret are not stored in the audit log or returned to the browser.

Testing: `npm.cmd run typecheck` passed, all 66 unit tests passed, and the production build passed with `/api/admin/twilio/test-message` present. Existing Twilio coverage proves the server uses the configured WhatsApp sender, approved `HX...` Content SID and two expected template variables. These local checks mock Twilio and do not prove sender registration, template approval or handset delivery. Commit/push, CI, deployment/configuration and live staging verification are pending at this entry. Provider completion requires the purchased Twilio number to be registered as a WhatsApp sender, two utility templates approved, the Account SID/auth token/sender/Content SIDs saved in staging, transactional WhatsApp enabled, and one accepted plus one ready message confirmed on a consenting test handset and in the Twilio message log. Production WhatsApp must remain disabled until that staging evidence exists. No Asana tracker is in scope.

### 2026-09-16 FOND-integrated Twilio Sandbox transport

Implementation: FOND Provider credentials now offers an explicit `FOND staging test · Twilio Sandbox` connection mode. In that mode the actual FOND order-notification adapter sends accepted and ready messages as free-form WhatsApp text through Twilio's fixed Sandbox sender; the controlled dashboard test uses the same adapter. Sandbox configuration is rejected unless FOND's validated public hostname is exactly `fond-test.mid-point.co.za`, so it cannot be enabled on production. The registered-sender path remains unchanged and continues to require the FOND sender plus two approved Content SIDs. Existing saved configurations without a mode inherit production mode.

Testing: `git diff --check`, `npm.cmd run typecheck`, all 67 unit tests and the production build passed. Added coverage proves the staging hostname gate, fixed sandbox sender, readable FOND order text and absence of a Content SID in sandbox requests; existing coverage still proves the production Content SID request. Provider calls are mocked, so handset delivery is not yet proved. Commit/push, merge, CI, deployment/configuration and live staging verification are pending at this entry. After deployment, the main Twilio Account SID and auth token must be saved only in staging, Sandbox mode selected, a consenting test phone joined to Twilio Sandbox, transactional WhatsApp enabled, and both the controlled test and a real opted-in staging order accepted/ready flow confirmed on the handset and in Twilio logs. Production WhatsApp remains disabled and the existing WABA/senders remain untouched. No Asana tracker is in scope.

Promotion follow-up (2026-09-16): implementation, tests and the initial evidence entry were committed and pushed directly to `origin/main` as `fd71dab` (no PR merge). Verify FOND run #`35088267696` passed on that exact head and Deploy FOND run #`35088454088` completed successfully. Live staging health returned `status=ok` in `yoco-sandbox` payment mode, and direct deployed asset readback contained the new Twilio Sandbox dashboard UI. No Twilio credential or provider switch was changed during deployment. Configuration and handset verification remain open: save the main-account SID/auth token in staging, choose Sandbox mode, join the test handset, enable transactional WhatsApp, then prove both accepted and ready messages from a real opted-in staging order and reconcile them in the Twilio message log. Production WhatsApp remains disabled and the existing WABA/senders remain untouched. The canonical `PROJECT_CONTEXT.md` was included in the release; this follow-up is documentation-only. No Asana tracker is in scope.

### 2026-09-21 Twilio SMS order notifications

Implementation: added SMS as a separate transactional channel while leaving WhatsApp unchanged. Customer collection and delivery checkout can opt in to accepted and ready SMS updates when SMS is enabled. FOND stores SMS consent separately, creates durable accepted/ready jobs, sends through Twilio from the configured E.164 sender, and records Twilio message IDs and queued/sent/delivered/undelivered/failed states. Ready text distinguishes collection from delivery. Twilio status callbacks are authenticated with the provider signature before updating a job. Super admins can save the SMS Account SID/sender, enable SMS independently, send one controlled test without creating an order, inspect the SMS log, and retry explicit failures. The shared Twilio auth token remains encrypted in the existing credential vault. Defaults keep SMS disabled on both production and staging until explicitly configured.

Testing: `npm run typecheck` passed; 74/74 unit tests passed, including SMS configuration validation, exact Twilio request fields, concise accepted/ready copy, consent-separated durable jobs, unconfigured-provider behavior and signed callback validation; `npm run build` passed with both `/api/admin/twilio/test-sms` and `/api/webhooks/twilio/sms`. The complete Playwright suite passed 40/40 across desktop and mobile against a fresh isolated SQLite database. A focused provider-access retest passed 2/2 and confirmed the super-admin provider response exposes an empty SMS configuration and the public callback URL. An earlier browser run used the repository's accumulated local database and failed order creation at the pre-existing kitchen-capacity limit; the same suite passed completely against the required clean database, so this was test-data state rather than an application failure. Twilio network calls were mocked and no real SMS was sent during local validation.

Commit/push: pending at this entry. Merge: direct-main promotion planned; no PR merge. Deployment/configuration: pending. After deployment, configure staging only with the verified main Twilio Account SID and sender `+27600928520`, confirm the encrypted Twilio auth token is present, and keep production SMS disabled. Live verification remains open: send one controlled staging SMS to Brett's consenting handset, confirm receipt and Twilio message-log delivery, then enable staging transactional SMS and exercise a real opted-in order through accepted and ready for both collection and delivery wording. Production enablement remains gated on that evidence and customer/restaurant UAT. Incoming customer SMS/reply handling is outside this transactional outbound scope. No Asana tracker is in scope.

SMS promotion and first live staging delivery follow-up (2026-09-21): implementation and the initial evidence entry were committed and pushed directly to `origin/main` as `85b89c7` (no PR merge). Verify FOND run #`35588661295` passed on that exact head, and Deploy FOND run #`35588860728` completed successfully for both production and isolated staging. Deployment did not enable production SMS.

Staging configuration: the existing encrypted Twilio auth token showed `Stored`; the verified main-account SMS sender `+27600928520` was saved and FOND reported SMS credentials as `Configured`. Transactional SMS was enabled in staging, retained after a full admin reload, and public `https://fond-test.mid-point.co.za/api/store` readback returned `settings.smsEnabled=true`. Production SMS remains disabled.

Live staging verification: Brett explicitly approved one controlled accepted-order test to his consenting handset at `+27817088120`. FOND reported that Twilio accepted the message as queued. The main-account Twilio Programmable Messaging log then showed the newest message as `Outbound API`, from `+27600928520` to `+27817088120`, one segment, status `Delivered`, with the exact body `FOND: Your order FOND-SMS-TEST has been accepted and is being prepared.` This proves the deployed staging application can submit an SMS through the intended FOND sender and that Twilio recorded carrier delivery. Brett has not yet independently confirmed the message on the physical handset.

Remaining gates: exercise a real opted-in staging order through accepted and ready states, prove the durable job/status-callback path, and verify both collection and delivery ready wording on the handset and in FOND's SMS log. Production enablement and customer/restaurant UAT remain open. Incoming customer SMS and reply handling remain outside this transactional outbound scope. The canonical context includes this evidence on `origin/main`; no Asana tracker is in scope.

### 2026-09-21 FOND transactional email domain correction

Implementation: corrected the Resend sender allow-list, admin placeholder and operator guidance from the unrelated `fond.co.za` domain to FOND's actual verified subdomain, `fond.mid-point.co.za`. The validation is centralized in `validEmailSender`, accepts only addresses on that exact subdomain and rejects both the old domain and the parent `mid-point.co.za` domain. Added a super-admin controlled email test that sends one fixed FOND staging message to an explicitly entered recipient through the same server-side Resend adapter, without creating a customer account or order; its audit record does not store the recipient address. Cloudflare DNS now contains Resend's DKIM and return-path records for `fond.mid-point.co.za`, and Resend reports the domain as `Verified`. A sending-only Resend API key restricted to `fond.mid-point.co.za` was created and saved into staging's encrypted provider vault; the key value is not recorded here.

Testing: `npm.cmd test` passed 76/76, including exact-domain validation and controlled-send request coverage; `npm.cmd run typecheck`, `npm.cmd run build` and `git diff --check` passed. The build includes `/api/admin/email/test-message`. These local tests mock Resend and do not prove provider delivery. The domain correction was committed and pushed directly to `origin/main` as `da5adc7` (no PR merge); Verify FOND run #`35594749556` passed in 2m 11s and Deploy FOND run #`35594949140` completed successfully in 1m 25s with both production and isolated staging jobs green. Staging then retained `Resend API key · Stored` and `orders@fond.mid-point.co.za` after a full admin reload. The controlled-test addition, its CI/deployment and real provider/mailbox delivery are pending at this entry. Order receipt/status email UAT and production configuration remain open. The mistaken pending `fond.co.za` entry in Resend has no DNS records and remains available for separate deletion approval. No Asana tracker is in scope.

Email test promotion and first live staging delivery follow-up (2026-09-21): the controlled-test implementation and initial evidence entry were committed and pushed directly to `origin/main` as `1cf43d7` (no PR merge). Verify FOND run #`35595562578` passed on that exact head in 2m 8s, and Deploy FOND run #`35595760097` completed successfully in 1m 20s with both production and isolated staging jobs green. Deployment did not configure or enable production email.

Live staging verification: after deployment, the authenticated FOND admin showed `Resend API key · Stored`, the verified sender remained `orders@fond.mid-point.co.za`, and the new controlled email panel was available. Brett explicitly approved one real test to `brettd@blendproperty.co.za`. FOND reported `Resend accepted the FOND test email`, and the Resend sending log recorded the newest message to that address with subject `FOND email test` and status `Delivered`. This proves the deployed staging application submitted the message through the intended sender and Resend recorded delivery. Recipient-mailbox appearance has not yet been independently confirmed.

Remaining gates: confirm the test in Brett's mailbox, then exercise a real staging customer account through email verification and a real opted-in order through receipt/status notifications, including failure/retry visibility. Production sender/key configuration and production email UAT remain open. The mistaken pending `fond.co.za` Resend domain entry has no DNS records and remains available for separate deletion approval. No Asana tracker is in scope.

### 2026-09-21 branded transactional email templates

Implementation: replaced FOND's raw one-line email bodies with compact, Outlook-safe table layouts using inline styles, a hidden preview line, a text-only fallback and no externally hosted images. The controlled test, six-digit verification and received/accepted/ready/completed order messages now share the FOND Midpoint brand header and accessible content hierarchy. Order emails include reference, status, fulfilment timing, destination, payment method, line items, modifiers and trusted stored total. Subjects now describe the customer action or order state, and the sender display name is `FOND Midpoint`. User-controlled values are HTML escaped before rendering.

Deliverability diagnosis: Resend's Insights report for the first live message marked every available check `Doing great`, including valid inherited DMARC, the verified sending subdomain, matching links, plain text, small body, non-`no-reply` sender and image rules. DNS readback also confirmed Resend DKIM and SPF. The first message's Outlook spam placement is therefore not evidence of failed authentication; the more likely remaining factors are a brand-new sending subdomain/sender reputation and mailbox-level trust. No DNS record was changed. Inbox placement cannot be guaranteed by application code, and Brett should mark the next legitimate message `Not junk` if Outlook still filters it.

Testing: all 76 unit tests pass with new assertions for the display sender, branded HTML, plain-text fallback, verification-code layout and actual order item/total content. `npm.cmd run typecheck`, the Next.js production build and `git diff --check` pass. The controlled test template was rendered to real HTML and visually inspected at 2560×1440 and 390×844: the centered desktop card and single-column mobile layout are readable, unclipped and preserve hierarchy and touch-safe CTA spacing.

Commit and push: commit `a6ecaa4` (`Design branded FOND email templates`) was pushed to `origin/main`; local `HEAD` and `refs/heads/main` both resolved to `a6ecaa42bb1af63c28e5a38a3f6ec40035e384c5` before this evidence update.

Merge: the repository deploys directly from `main`; no separate pull request or merge was required for this authorized change.

Deployment and configuration: Verify FOND run `#123` (`35597643561`) completed successfully in 2m16s. Deploy FOND run `#69` (`35597860577`) completed successfully in 1m36s; its production job passed in 44s and staging job passed in 45s. After deployment, the authenticated staging admin loaded successfully, showed `Resend API key · Stored`, and exposed the controlled email test.

Live verification and remaining gates: the deployed staging configuration and test surface are verified. A second real email using the redesigned template is prepared but has not yet been sent, so Resend acceptance/delivery and Outlook rendering/inbox placement for the new design remain open. Production email configuration and real customer verification/order lifecycle UAT also remain open. No Asana tracker is in scope.

### 2026-09-21 receipt-format transactional email revision

Implementation: revised the transactional email system against Brett's supplied receipt reference. Controlled tests, verification codes and order updates now use a narrow centred receipt layout with a compact FOND mark, blue status banner, dense customer/order metadata, item and price columns, subtotal/payment/total/amount-due rows, operational guidance, a FOND menu panel and a blue help footer. Order messages render the stored customer name, email, phone, date, fulfilment, destination, payment state, notes, item modifiers and reference. The controlled test includes the actual recipient address so its structure can be reviewed without creating a fake order. The implementation remains table-based with inline styles, escaped user data, a hidden preview line and a complete plain-text fallback for Outlook compatibility.

Testing: all 76 unit tests pass, including receipt-structure, recipient, order-detail and amount-due assertions. TypeScript validation, the Next.js production build and `git diff --check` pass. The actual order and controlled-test HTML were rendered locally and visually inspected: the reference-style blue banner, two-column receipt details, aligned item prices/totals and FOND promotional panel render without clipping. Commit/push, CI, deployment and live Resend/Outlook verification are pending at this entry. Production email configuration and real customer order lifecycle UAT remain open. No Asana tracker is in scope.

Receipt-format promotion follow-up (2026-09-21): the implementation and initial evidence entry were committed and pushed directly to `origin/main` as `9811254` (`Match FOND emails to receipt layout`); the direct-main workflow required no separate pull request or merge. Verify FOND run #`35599616053` passed in 2m00s. Deploy FOND run #`35599807584` passed in 1m33s, with the production job green in 39s and the isolated staging job green in 47s. No provider credential or email switch changed during deployment.

Live staging verification: after deployment, the authenticated FOND admin loaded successfully, showed `Resend API key · Stored`, retained the verified sending-address control, and exposed the controlled email test. Brett explicitly approved one receipt-format test to `brettd@blendproperty.co.za`. FOND reported `Resend accepted the FOND test email`, and the Resend delivery record `01a0c3fd-1195-70fb-9059-f16c0a258303` confirmed sender `FOND Midpoint <orders@fond.mid-point.co.za>`, subject `Your FOND email updates are ready`, and status `Delivered` at 14:42 on 2026-09-21. The provider preview displayed the deployed blue status banner, recipient/sender/status details, FOND operational panel, menu call-to-action and help footer. Outlook rendering and inbox-versus-junk placement still require recipient-mailbox confirmation. Production email configuration and real customer verification/order lifecycle UAT remain open. No Asana tracker is in scope.

### 2026-09-21 Messaging Studio for transactional email and SMS

Implementation: added a protected Messaging Studio under Marketing & CMS. Administrators can edit five order-email stages and three SMS stages using a constrained set of order placeholders, see an immediate sample preview, save a draft, publish it for future messages and restore the previous publication. Email controls cover subject, blue status banner, customer wording, footer and an optional promotional panel with image upload, alt text, heading, message and safe FOND/HTTPS call-to-action. SMS controls show the rendered character count and retain the order reference. Published templates are validated server-side and used by the existing transactional email and SMS adapters; invalid or missing stored documents fail back to the tested defaults. WhatsApp remains visibly provider managed because business-initiated templates require approval in Meta or Twilio. The studio explicitly limits these templates to opted-in transactional order messages and records that promotional campaigns require separate marketing consent and unsubscribe handling.

Testing: `npm.cmd run typecheck` passed, all 78 unit tests passed and the Next.js production build passed. New coverage proves supported placeholder rendering, rejection of unsupported placeholders and unsafe banner links, required order references, published custom email and SMS content, absolute uploaded-image URLs and escaping of customer-controlled values. `git diff --check` passed. The local authenticated admin was inspected at desktop and 1024×768 tablet sizes: Email and SMS editors, stage selectors, live sample previews, draft/live state and publish/restore controls rendered correctly; an unsaved heading change immediately updated the preview and was reverted without persistence; browser console errors were empty. No provider message was sent during this feature validation.

CI correction: the first Verify FOND run `35603063863` passed typecheck, all 78 unit tests and the production build, then failed two copies of the existing CMS browser test because the new studio introduced a second button named exactly `Save draft`; 38/40 browser tests passed. The message button was renamed `Save message draft` to preserve the existing CMS control's unique accessible name and make the two actions clearer. After a fresh production build, the complete Playwright suite passed 40/40 across desktop and mobile using a new isolated SQLite database. Replacement CI evidence is pending at this entry; the failed run did not deploy.

Commit and push: pending at this entry. Merge: direct-main promotion planned; no separate pull request is required by the repository workflow. Deployment and configuration: pending; no Resend, Twilio, email or SMS provider setting is changed by the release. Live staging verification: pending deployment and authenticated readback of Marketing & CMS. Publishing new copy, sending bulk/promotional campaigns, WhatsApp provider-template approval, production email/SMS configuration and real customer lifecycle UAT remain open gates. No Asana tracker is in scope.

Promotion follow-up (2026-09-22): the studio implementation and initial evidence were committed and pushed directly to `origin/main` as `7fbd8db`; the accessible-name correction and clean 40/40 browser-suite evidence were pushed as `907cb34` (no separate PR merge). Replacement Verify FOND run `35692785095` passed on the exact `907cb34` head. Deploy FOND run `35692918749` completed successfully with both `deploy` and `deploy-staging` green. No provider credential, enablement switch or published message template changed during deployment. Public readback from both `https://fond.mid-point.co.za/admin` and `https://fond-test.mid-point.co.za/admin` returned HTTP 200 with healthy APIs, and their currently served JavaScript asset contained the deployed `MESSAGING STUDIO`, `Save message draft` and `Publish to future messages` controls. This proves the exact release is served on production and staging. A fresh browser session reached the protected staging Unlock screen, so authenticated post-deploy interaction inside Marketing & CMS remains an explicit operator-session check; no draft or publication was changed. Bulk/promotional campaign sending, WhatsApp provider-template approval, production provider configuration and real customer lifecycle UAT remain open gates. No Asana tracker is in scope.

### 2026-09-22 staging-to-production operational configuration consolidation

Implementation: added a guarded, manual GitHub Actions workflow and two one-time server scripts that export only operational configuration from isolated staging and import it into production through a direct container pipe. The package includes the complete editable menu, promotion image library, published/draft/previous customer content and message documents when present, email sender, Twilio SMS and Meta configuration, encrypted provider credentials that are valid across environments, and missing team accounts. Credentials are decrypted only inside the staging container and immediately re-encrypted with the distinct production vault key; their values are not written to workflow logs or repository files. Production orders, customers, accounts, sessions, payments, notification jobs and audit history are excluded. Existing production team accounts and 2FA are preserved to prevent an access lockout. Both SQLite databases are backed up before each consolidation attempt.

Production safeguards: staging Yoco test credentials/webhook and Twilio WhatsApp Sandbox configuration are never promoted as live configuration. The importer always forces `allowTestPayments=false`; online checkout is enabled only if production already has a live Yoco key and signing secret. WhatsApp is enabled only when production has a complete Meta connection or registered Twilio sender plus approved Content SIDs. SMS is enabled only when the copied Account SID, E.164 sender and encrypted Twilio token form a complete configuration. The importer requires both an explicit production target and exact `fond.mid-point.co.za` host guard.

Testing: the new integration test builds separate staging and production SQLite databases with different vault keys, exports/imports the operational package, and proves menu/image/document transfer, credential re-encryption, SMS enablement, preservation of the existing production super-admin and Yoco credential, rejection of staging sandbox WhatsApp, and forced disabling of test/online payments. `npm.cmd test` passed 81/81, `npm.cmd run typecheck`, `npm.cmd run build` and `git diff --check` passed.

Commit and push: implementation commit `d50af60978c2267403722887e935659310d80e3b` and hostname-guard correction `d11e9688fbced8c72682b75f6c3dc8b28fcf714f` were pushed directly to `origin/main`. Merge: this repository uses authorized direct-main promotion; no separate pull request or merge was used.

Deployment and configuration: Verify FOND run `35699669210`, staging deploy `35699820298` and production promotion `35699963931` passed for `d50af60`. The first consolidation run `35700069386` created both backups but stopped before opening or mutating production because `FOND_PUBLIC_URL` was not exposed inside the container. The exact host was then passed explicitly while retaining the guard. Replacement Verify FOND run `35700206226`, staging deploy `35700386744`, production promotion `35700476190` and consolidation run `35700563991` all passed for `d11e968`. The successful import reported 162 stored menu rows, zero promotion images, four present operational documents, no new team accounts, and re-encrypted `email-api`, `twilio-auth-token` and `meta-webhook-verify` credentials. Its health check returned `status=ok` with live ordering and in-person payments.

Live production verification: public store readback returned `settings.smsEnabled=true`, `settings.whatsappEnabled=false`, `settings.allowTestPayments=false`, `onlinePayments=false` and `paymentMode=none`. Production and staging public menus each exposed the same 140 available items with matching ordered IDs, prices and availability; the database import count is higher because 22 intentionally hidden items remain editable in Admin. The authenticated production Settings page retained Brett Dovey's active named super-admin and 2FA, displayed the consolidated 162-row menu, `Resend API key · Stored`, Twilio auth token stored, SMS sender `+27600928520`, SMS credentials `Configured` and transactional SMS enabled. It also recorded the `configuration-consolidated · staging-to-production` audit event at 09:38 SAST. No live email or SMS was sent during this migration.

Open provider and UAT gates: Delivery remains unavailable to customers because production still has no merchant-authorized live Yoco key/webhook; its stored test credential remains intentionally inert. WhatsApp remains disabled because Meta permanent access/app credentials and phone/WABA IDs are absent and the Twilio FOND sender/templates are not production-ready. A real production email verification/order lifecycle and opted-in SMS accepted/ready lifecycle still require controlled UAT and recipient/provider-log confirmation. WhatsApp approval, real customer/restaurant UAT, tablet/PWA testing, training and backup-restore rehearsal remain open. No Asana tracker is in scope.

### 2026-09-22 controlled Yoco sandbox checkout on the production domain

Implementation: FOND can now expose Yoco test checkout on the two exact approved hosts, `fond-test.mid-point.co.za` and `fond.mid-point.co.za`, only when a stored test key, stored webhook signing secret and the super-admin `Enable customer Yoco TEST checkout and sandbox webhook processing` switch are all present. Every other host and a disabled switch fail closed. The ordinary live-payment path remains separate and still requires a live key plus live-checkout enablement. Customer pages show a prominent `YOCO TEST PAYMENT MODE` banner, the basket identifies test checkout before submission, and test-specific button/status text states that no real money is charged. The staff tablet shows a persistent warning to treat sandbox-confirmed orders as tests and exclude them from live takings. Provider and Settings guidance now describes the production-domain test option and its safeguards.

Testing: `git diff --check`, TypeScript validation, all 82 unit tests and the Next.js production build passed. New unit coverage proves production and staging enter sandbox mode only with the explicit switch and required test credentials, while unrelated hosts and a disabled switch remain off. The first browser-suite run reused the repository's accumulated local database and reached its existing kitchen-capacity limit; its error readback explicitly reported that capacity condition. The complete Playwright suite then passed 40/40 across desktop and mobile against a fresh isolated SQLite database. No Yoco provider call or payment was made during local validation.

Commit/push: pending at this entry. Merge: direct-main promotion planned; no separate pull request is required by the repository workflow. Deployment/configuration: pending. After the verified release is deployed, the production super-admin switch must be enabled and saved, then anonymous `/api/store`, the public basket and the authenticated staff tablet must prove `paymentMode=sandbox`, online checkout availability, selectable Delivery and the visible warnings. A test order/payment has not yet been created. Live Yoco launch remains gated on merchant-authorized `sk_live_` credentials, a production signing secret, switching sandbox back off, enabling live checkout, a controlled real charge/refund, finance reconciliation and customer/restaurant UAT. No Asana tracker is in scope.

Production sandbox activation follow-up (2026-09-22): implementation and its initial evidence were committed and pushed directly to `origin/main` as `2f473b3295ff370ad389394283e2f19482be98a7` (`Enable guarded Yoco sandbox checkout on production`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35702579058` passed on that exact commit, Deploy FOND Staging run `35702764260` passed, and Promote FOND Production run `35702866339` completed successfully.

Deployment and configuration: after production promotion, the authenticated production Settings page displayed the new switch, retained the stored Yoco test key and webhook signing secret, and reported credential mode `test` with notification URL `https://fond.mid-point.co.za/api/payments/webhook`. The super-admin switch was enabled and saved successfully. The reloaded page showed the switch checked, `Test payments are active`, Yoco credentials `Configured`, and an audited `save · trading` event at 10:06 SAST. The separate live-checkout switch remains off.

Live production verification: anonymous `https://fond.mid-point.co.za/api/store` readback returned `allowTestPayments=true`, `onlinePayments=true`, `paymentMode=sandbox`, `deliveryEnabled=true`, SMS enabled and WhatsApp disabled; `/api/health` returned `status=ok`, live ordering and `payments=yoco-sandbox`. The served customer page displayed the `YOCO TEST PAYMENT MODE` warning. In a one-item basket, Delivery was enabled and selectable; selecting it displayed the delivery address fields, checked `Use Yoco TEST checkout`, and changed the action to `Continue to Yoco TEST checkout` with an explicit no-real-money notice. The authenticated staff tablet displayed the persistent `YOCO TEST MODE` warning instructing staff to exclude test confirmations from live takings. The customer basket and staff queue were left open for Brett's review. No order was submitted and no Yoco checkout, payment, email, SMS or WhatsApp message was created during this activation.

Open gates: the guarded production sandbox is now active for controlled user/staff testing, but an actual end-to-end test order still needs Brett's chosen test details and completion through Yoco's sandbox, followed by webhook reconciliation, staff acceptance, POS-reference/preparing/ready/completed transitions and customer tracking confirmation. Live launch still requires merchant-authorized `sk_live_` credentials, a live production webhook/signing secret, disabling sandbox mode, enabling live checkout, a controlled real charge and refund, finance reconciliation and customer/restaurant UAT. The current customer test banner and staff warning must remain until sandbox is disabled. WhatsApp provider approval, tablet/PWA device UAT, staff training and backup-restore rehearsal remain open. No Asana tracker is in scope.

### 2026-09-22 disable unavailable WhatsApp checkout opt-in

Implementation: customer collection and delivery checkout now keep the WhatsApp notification option visible but greyed out and disabled while `whatsappEnabled=false`, with the explicit label `WhatsApp notifications unavailable — setup pending`. A disabled provider also clears any previously selected WhatsApp opt-in and the order payload is forced to `whatsappOptIn=false`, preventing a stale browser state from recording an unavailable preference. When transactional WhatsApp is eventually configured and enabled, the normal opt-in wording and phone-number gate return automatically. SMS remains independent and available.

Testing: `git diff --check`, TypeScript validation, all 82 unit tests and the Next.js production build passed. The complete Playwright suite passed 40/40 across desktop and mobile against a fresh isolated SQLite database; the checkout test now proves the unavailable WhatsApp control remains disabled even after a valid contact number is entered. No provider call or message was made. Commit/push, merge, deployment/configuration and live production verification are pending at this entry. Required live evidence is production promotion and a basket check after entering a contact number. WhatsApp must remain disabled in production until the sender, approved templates and real delivery UAT are complete. No Asana tracker is in scope.

Promotion and live verification follow-up (2026-09-22): implementation and its initial evidence were committed and pushed directly to `origin/main` as `877ff358f5440f9bfa5c8d5746ca5179cc7b0632` (`Disable unavailable WhatsApp checkout opt-in`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35704062897`, Deploy FOND Staging run `35704249880` and Promote FOND Production run `35704340117` all passed for that exact commit. Deployment preserved the production sandbox-payment and SMS configuration and did not enable or configure WhatsApp.

Live production verification: the public production basket displayed `WhatsApp notifications unavailable — setup pending` as a disabled checkbox. After entering the isolated test contact number `0821234567`, the SMS checkbox became enabled while the WhatsApp checkbox remained disabled, proving that the control is tied to provider availability rather than only the presence of a phone number. The basket was left open for Brett's review. No order was submitted and no payment, SMS, email or WhatsApp message was sent. Production WhatsApp remains disabled until the sender, approved templates and end-to-end delivery evidence are complete. No Asana tracker is in scope.

### 2026-09-22 preselected customer email and SMS order updates

Implementation: collection and delivery checkout now show SMS, email and WhatsApp notification choices together. SMS is selected by default and becomes available when a contact number is present. A signed-in customer with a verified email address sees `Email me when my order is accepted and ready` selected by default with the destination address shown; signed-out and unverified customers see a greyed explanation directing them to sign in or verify in My account. WhatsApp retains the separate greyed setup-pending state. The email choice is stored on the order and enforced for the received, accepted, ready and completed email lifecycle, so clearing it creates no email delivery job. The server accepts email opt-in only from a verified signed-in session and does not trust a browser-supplied address.

Testing: `git diff --check`, TypeScript validation, all 82 unit tests and the Next.js production build passed. Unit coverage proves an opted-out verified order creates no email job. The first browser run passed 36/40 but its clean default database correctly had SMS disabled, so four new assertions could not locate the provider-gated SMS control. The assertions were moved into a focused provider-state scenario. The complete replacement Playwright suite passed 42/42 across desktop and mobile against a fresh isolated SQLite database, proving verified email and configured SMS are both preselected, SMS stays selected when a contact number enables it, unverified/guest email is disabled with clear guidance, and unavailable WhatsApp remains disabled. No order, provider call or message was sent by the focused notification-choice test.

Commit and push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment and configuration: pending and no provider credential or enablement switch is changed by this release. Live production verification must confirm the currently configured SMS option is preselected, the correct signed-in verified account sees email preselected, WhatsApp remains unavailable, and clearing either available choice is respected. Real production email and SMS accepted/ready lifecycle delivery remains a controlled UAT gate; WhatsApp sender/template approval and delivery UAT remain open. No Asana tracker is in scope.

Promotion and live verification follow-up (2026-09-22): implementation and its initial evidence were committed and pushed directly to `origin/main` as `4eaabc4b3646d0d2135871e75f24920499b86982` (`Preselect email and SMS order updates`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35705526226`, Deploy FOND Staging run `35705737108` and Promote FOND Production run `35705831564` all passed for that exact commit. No provider credential, channel switch or message template changed during deployment.

Live production verification: the anonymous production basket showed SMS already checked but disabled before a contact number, the explicit greyed email guidance `Email notifications unavailable — sign in and verify your email`, and WhatsApp still greyed as setup pending. Entering the isolated test number `0821234567` enabled SMS while preserving its checked state; email and WhatsApp remained disabled for the signed-out session. The focused desktop/mobile browser validation proves that a verified signed-in session instead shows its destination email and has that email checkbox checked and enabled. A live verified customer session was not used in production, so an actual opted-in email/SMS accepted-and-ready lifecycle and recipient/provider-log confirmation remain controlled UAT gates. The basket was left open for Brett's review. No order was submitted and no payment, email, SMS or WhatsApp message was sent. No Asana tracker is in scope.

### 2026-09-22 reject duplicate Yoco POS references

Implementation: staff Yoco POS recording now checks the trimmed reference inside the existing immediate SQLite transaction before writing the order. If another order already holds the same reference, including a case-only variation such as `YOCO-123` versus `yoco-123`, the server rejects it and identifies the FOND order that already owns the reference. The attempted second order remains unchanged and receives no POS-recorded audit event. The immediate transaction serializes competing tablet writes, so two staff devices cannot successfully claim the same new reference concurrently. Existing production records are not rewritten and startup is not blocked by any historic test data.

Testing: `git diff --check`, TypeScript validation, all 82 unit tests and the Next.js production build passed. The unit test records one reference, rejects the same reference with different case and surrounding spaces on a second order, and proves the second order remains unrecorded. The first unit run correctly exposed two old analytics fixtures that deliberately reused one static fake reference; those fixtures were changed to order-specific references. The first full browser run then passed 39/42 and exposed the same fixed-reference collision between parallel desktop/mobile projects in three browser fixtures. After making legitimate fixtures order-specific while retaining the deliberate duplicate attempt, the complete replacement Playwright suite passed 42/42 across desktop and mobile against a fresh isolated SQLite database. No real order, Yoco record, payment or provider call was made.

Commit and push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment and configuration: pending and no provider credential or setting is changed. Live functional proof will be retained at test/staging scope to avoid writing artificial orders into production; production verification requires the exact tested commit to pass CI, staging deployment, production promotion and health checks. Staff operational UAT should confirm a real Yoco reference cannot be reused when two legitimate orders are processed. No Asana tracker is in scope.

Promotion and production verification follow-up (2026-09-22): the implementation and initial evidence were committed and pushed directly to `origin/main` as `db6f8f8aeeceb5545fa37b231d6790254aca7941` (`Reject duplicate Yoco POS references`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35706869991`, Deploy FOND Staging run `35707057048` and Promote FOND Production run `35707144490` all passed for that exact commit. Production `/api/health` returned `status=ok` with Yoco sandbox payment mode, and public store readback retained SMS enabled and WhatsApp disabled. No provider configuration changed. The duplicate-reference behavior is proven by unit and full HTTP browser suites; no artificial orders were written to production for the live check. A real staff two-order duplicate attempt remains an explicit operational UAT step. No Asana tracker is in scope.

### 2026-09-22 direct guest email entry at checkout

Implementation: removed the account-verification gate from transactional checkout email. Every collection and delivery customer now sees an `Email address` field and an enabled, preselected `Email me when my order is accepted and ready` choice. A signed-in account pre-fills its address, but signed-out and unverified customers can enter a different valid address directly. The order endpoint normalizes the selected address, rejects missing or invalid addresses when email updates remain selected, includes the address in idempotency matching, and creates received/accepted/ready/completed email jobs for any opted-in order. Turning email updates off creates no email job and a signed-out guest address is not retained by the order API. SMS remains preselected and becomes enabled after a contact number is entered; WhatsApp remains visibly greyed out while its provider is unavailable. Account verification remains available to prove account ownership but is no longer required for transactional order updates.

Testing: `git diff --check`, TypeScript validation, all 83 unit tests and the Next.js production build passed. Unit coverage proves guest address validation and normalization, one email per order event, retry idempotency, verified-account delivery and no job after opt-out. The first Playwright run passed 39/42: two retry-order scenarios correctly stopped because the new required opted-in email had not been entered, and one unrelated promotion popup test hit a timing race. The retry fixture was updated, then the complete replacement Playwright suite passed 42/42 across desktop and mobile against a fresh isolated SQLite database. Browser coverage proves an anonymous customer can enter an address and place/track an order, an unverified signed-in customer is prefilled and enabled, Email and configured SMS start selected, and unavailable WhatsApp stays disabled. Provider calls are mocked in automated tests.

Commit and push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment/configuration: pending; no provider credential, payment mode, SMS setting or WhatsApp setting changes are included. Live production verification must confirm the anonymous basket shows the direct email field plus selected Email/SMS and disabled WhatsApp without submitting an order. A real production email and SMS accepted/ready lifecycle and recipient/provider-log reconciliation remain controlled UAT gates. No Asana tracker is in scope.

Promotion and live production verification follow-up (2026-09-22): implementation, automated tests and the initial evidence entry were committed and pushed directly to `origin/main` as `9c26ef4138870e871c2601ce083727794fd919ea` (`Enable direct checkout email updates`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35710417602` passed on that exact commit, Deploy FOND Staging run `35710694884` passed, and Promote FOND Production run `35710891899` completed successfully. No provider credential, payment mode or channel enablement setting changed during deployment.

Live production verification: an anonymous cache-busted basket on `https://fond.mid-point.co.za/` displayed the direct `Email address` field and an enabled, checked `Email me when my order is accepted and ready` checkbox. SMS was checked but disabled before a contact number; after entering the isolated test number `0821234567`, SMS became enabled and remained checked. WhatsApp remained disabled with `WhatsApp notifications unavailable — setup pending`. The reserved non-routable address `guest@example.test` was entered to confirm the field accepts a normal email address. The basket was left open for Brett's review and was not submitted, so no production order, Yoco checkout/payment, email, SMS or WhatsApp message was created. A real opted-in production order through accepted and ready, with recipient receipt and email/SMS provider-log reconciliation, remains the explicit controlled UAT gate. WhatsApp remains unavailable pending provider approval and delivery UAT. No Asana tracker is in scope.

### 2026-09-22 visible duplicate Yoco reference warning

Implementation: the staff Yoco POS dialog now tells the operator before submission that each receipt or order number can be used only once. If the server rejects a duplicate, the dialog remains open and displays a high-contrast inline error naming the FOND order that already owns the reference, followed by guidance to check the receipt and enter a different number. Editing the reference, cancelling or reopening the dialog clears the previous error. The existing transactional, case-insensitive duplicate guard remains authoritative.

Testing: `git diff --check`, TypeScript validation, all 83 unit tests and the Next.js production build passed. Focused staff UI and HTTP coverage passed 4/4 across desktop and mobile and proves the advance warning, case-insensitive duplicate rejection, owning-order reference and persistent correction dialog. The first complete browser run passed 41/42 with one unrelated mobile offline-fallback timing failure; the complete replacement suite passed 42/42 against another fresh isolated SQLite database. Automated tests used isolated databases and made no real order, Yoco POS or provider call.

Commit and push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment and configuration: pending; no provider credential or operational setting changes are included. Live production verification should confirm the exact tested release and served warning/error copy without creating artificial production orders. A real staff two-order duplicate attempt remains an explicit operational UAT step. No Asana tracker is in scope.

Promotion and live production verification follow-up (2026-09-22): the implementation, automated tests and initial evidence entry were committed and pushed directly to `origin/main` as `109c91a2fe8622e580def0f1ee0ae2db5ef561cf` (`Show duplicate Yoco reference errors`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35712279485` passed in 2m16s, Deploy FOND Staging run `35712496383` passed in 54s, and Promote FOND Production run `35712678797` passed in 57s for that exact commit. No provider credential or operational setting changed during deployment.

Live production verification: `https://fond.mid-point.co.za/api/health` returned `status=ok` with `payments=yoco-sandbox`. A cache-busted production `/staff` readback and its served JavaScript asset `/_next/static/chunks/2c552lq_hgdza.js` contained the advance warning `Each Yoco receipt or order number can only be used once`, proving the released staff dialog contains the new guidance. The server-supplied duplicate rejection wording and persistent correction dialog are proved by the full HTTP and browser suites rather than a live duplicate submission. No artificial production order or Yoco reference was created for the live check. A real staff two-order duplicate attempt remains the explicit operational UAT step. No Asana tracker is in scope.

### 2026-09-22 two-message customer email journey and ready-order upsell

Implementation: customer order email now follows one clear two-message journey: `Order received` when the order is created, followed by either `Ready for collection` or `Ready for delivery` when staff move it to ready. Staff acceptance, preparation and completion remain operational workflow stages, but acceptance and completion no longer trigger duplicate customer email. The checkout opt-in now says `Email me when my order is received and ready`. The received email retains the blue receipt treatment and tells the customer that FOND will email again when the order is ready. Ready emails use a visually distinct dark-green `ORDER READY` status panel, state the correct collection/delivery handoff, retain the order reference and receipt details, and alone include the configurable next-order promotional banner. Messaging Studio now exposes only received, ready-collection and ready-delivery customer email templates, explains the lifecycle, labels the promotion as a ready-email upsell, and previews the different ready treatment. Legacy stored default subjects, ready wording and banner copy are upgraded on read without overwriting customized values.

Testing: `git diff --check`, TypeScript validation, all 85 unit tests and the Next.js production build passed. Focused email tests prove the automatic policy returns events only for received and ready; accepted, preparing, completed and cancelled return no customer-email event. Template coverage proves delivery-ready wording, the dark ready treatment, HTML escaping, ready-only banner rendering, and legacy-default upgrade. The first Playwright run reused an old local test server whose database was already at kitchen capacity, causing order fixtures to be rejected; it was discarded. A replacement run against a fresh isolated SQLite database passed 42/42. After adding explicit Messaging Studio coverage, a focused desktop/mobile run passed 2/2 and the final complete replacement suite passed 44/44 across desktop and mobile. It proves the three active email choices, distinct ready preview, ready-only upsell, updated checkout wording and the existing order flows. Automated provider calls are mocked or unavailable; no real order, payment, email, SMS or WhatsApp message was created.

Commit and push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment and configuration: pending; this release changes no provider credentials, payment mode, SMS switch or WhatsApp switch. Live verification must confirm the exact tested commit is promoted, production health remains green, the public checkout shows the new received-and-ready opt-in wording, and the served admin bundle contains the two-message journey and ready-only upsell treatment. A real opted-in customer order with recipient inbox and provider-log confirmation for both the received and correct ready message remains controlled production UAT. WhatsApp provider approval, live Yoco activation, tablet/PWA device UAT, staff training and backup-restore rehearsal remain open. No Asana tracker is in scope.

Promotion and live production verification follow-up (2026-09-22): implementation, automated tests and the initial evidence entry were committed and pushed directly to `origin/main` as `58fb869f2c72e6fcf2633f63736d61bdeb501021` (`Clarify received and ready order emails`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35715071125` passed in 2m54s, Deploy FOND Staging run `35715338812` passed in 1m00s, and Promote FOND Production run `35715511101` passed in 58s for that exact verified commit. No provider credential, payment mode or channel switch changed.

Live production verification: `https://fond.mid-point.co.za/api/health` returned `status=ok`, live ordering and `payments=yoco-sandbox`; public store readback retained SMS enabled and WhatsApp disabled. A cache-busted production basket displayed the enabled, selected `Email me when my order is received and ready` control. The released customer JavaScript contained that exact wording. The released admin assets contained the two-message lifecycle explanation, `Include this banner in ready emails` and `ORDER READY`, proving the production build includes the distinct ready preview and ready-only upsell controls. The basket was left without submission, so no production order, Yoco checkout/payment, email, SMS or WhatsApp message was created. Actual recipient delivery of one received email followed by the correct collection/delivery-ready email remains controlled UAT with provider-log reconciliation. No Asana tracker is in scope.

### 2026-09-22 delivery dispatch tracking, friendly order numbers and tablet search

Implementation: corrected the customer tracking model so fulfilment and stage control the wording. A collection order at `ready` shows `Ready for collection` without a stale kitchen estimate. A delivery order at `ready` shows `Ready for delivery — waiting for dispatch`; staff must use the new `Send out for delivery` action before the customer sees `Out for delivery`. The out-for-delivery view shows an expected arrival time calculated from the configured delivery target and the actual dispatch timestamp, and delivery cannot be marked complete before dispatch. Existing received/ready email and SMS policy remains unchanged, so dispatch does not create a third or duplicate notification.

Every order now receives a unique customer-facing number in the form `FOND-7K3P-9Q8R`. The original 32-character UUID reference remains the internal Yoco, checkout and webhook identifier. Existing database orders are backfilled safely at startup and the new customer number has a unique index. Customer confirmation, tracking, My account, transactional email, SMS/WhatsApp payloads, staff cards, browser alerts, duplicate-Yoco errors and Admin order rows use the friendly number. Tracking accepts either the friendly number or the legacy internal reference for backward compatibility.

The authenticated staff tablet now has a prominent `Find any order` search across current and historic records. Staff can search by friendly or internal order number, customer name, mobile number, company, building or Yoco POS reference, then see fulfilment, status, contact/destination, total, items and the permanent audit trail. The board has a separate `Out for delivery` lane and preserves payment and POS controls.

Testing: `git diff --check`, TypeScript validation, all 86 unit tests and the Next.js production build passed. Unit coverage proves friendly-number creation/lookup, delivery dispatch sequencing, collection protection, historic search and the seventh queue lane. The first Playwright run used the repository's accumulated local database and correctly hit the configured kitchen-capacity guard; Playwright is now pinned to a fresh in-memory SQLite database so browser evidence cannot be polluted by prior test orders. The final complete Playwright suite passed 46/46 across desktop and mobile. It proves customer out-for-delivery/ETA wording excludes collection and preparation copy, friendly-number public lookup, authenticated staff history search, and the existing full staff POS/payment lifecycle. Automated tests made no provider call or production order.

Commit and push: pending at this entry. Merge: authorized direct-main promotion is planned; no separate pull request is required by the repository workflow. Deployment and configuration: pending. Live production verification must prove the exact tested commit is promoted, the startup migration assigns friendly numbers to existing orders, the screenshot order shows delivery-specific ready wording, the staff tablet can find it by friendly number/name/mobile, and the dispatch action changes tracking to an arrival estimate. No production status will be changed solely for verification; a controlled delivery order remains required for the full user/staff handoff UAT. Live Yoco activation, WhatsApp approval, physical tablet/PWA UAT, staff training and backup-restore rehearsal remain open. No Asana tracker is in scope.

Promotion and live production verification follow-up (2026-09-22): implementation, automated tests and the initial evidence entry were committed and pushed directly to `origin/main` as `4402d5c38a9c8004c3ebf21b1dabe133e34d0dfe` (`Fix delivery tracking and order lookup`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35717359800` passed in 1m57s, Deploy FOND Staging run `35717540531` passed in 45s, and Promote FOND Production run `35717668323` passed in 53s for that exact commit. No provider credential, payment mode or notification-channel setting changed.

Live production verification: `https://fond.mid-point.co.za/api/health` returned `status=ok` with `payments=yoco-sandbox`. The released customer assets contain `Ready for delivery — waiting for dispatch` and `Out for delivery`; the released staff assets contain `Send out for delivery`, `Find any order`, the multi-field search prompt and the separate `Out for delivery` lane. Startup migration assigned the screenshot order the friendly number `FOND-T8XY-UZJM`, and a public lookup by its original reference returned fulfilment `delivery`. That historic order is currently `cancelled`, so its status was not changed to manufacture dispatch evidence or rewrite the audit history. No production order, payment, email, SMS or WhatsApp message was created and no production order status was changed during verification.

Open UAT gate: create one controlled delivery order, progress it on the physical tablet through ready, `Send out for delivery` and delivered, and confirm that customer tracking changes from waiting for dispatch to `Out for delivery` with the expected arrival time. The automated desktop/mobile suite already proves that sequence and the authenticated historic-order search, but actual tablet/PWA use and the real user/staff handoff remain outstanding. Live Yoco activation, WhatsApp approval, staff training and backup-restore rehearsal also remain open. No Asana tracker is in scope.

### 2026-09-22 prominent kitchen change instructions

Implementation: replaced the low-contrast parenthetical modifier sentence on staff tickets with a bordered `CHANGES TO MAKE` panel directly beneath the affected item. Menu wording is translated into explicit `REMOVE`, `SWAP`, `ADD` or fallback `CHANGE` rows while retaining the full requested detail; for example, `Vegan swap: smoky hummus instead of cheese` renders as `SWAP · Smoky hummus instead of cheese`, and `No pickled red onion` renders as `REMOVE · Pickled red onion`. Each action has a text label as well as a distinct colour, so the instruction does not depend on colour alone. The same treatment appears in historic staff search results. Free-form order notes now render in a separate amber `CUSTOMER NOTE` panel so allergies and handoff instructions are not left as small italic text.

Testing: `git diff --check`, TypeScript validation, all 87 unit tests and the Next.js production build passed. The modifier unit test proves removal, swap, add and fallback wording. A focused Playwright run initially timed out because its browser worker already held an authenticated tablet session and the test waited for a login field that was correctly absent; the fixture was changed to support both locked and already-authenticated states. The replacement focused run passed 2/2 across desktop and mobile, and the complete browser suite passed 48/48. It proves the ticket displays `CHANGES TO MAKE`, the exact swap/removal details and the separate customer-note warning on both screen sizes. Automated tests use an isolated database and created no production order or provider activity.

Commit/push: pending at this entry. Merge: the authorized direct-main workflow requires no separate pull request or merge. Deployment/configuration: pending; no menu data, provider credential, payment mode or notification setting changes are included. Live verification must confirm the exact tested build is promoted and the production staff asset contains the new instruction copy. A physical-tablet kitchen readability check with a controlled modified order remains operational UAT. No Asana tracker is in scope.

Promotion and live production verification follow-up (2026-09-22): implementation, automated tests and the initial evidence entry were committed and pushed directly to `origin/main` as `ef9a98f467a53b52eec11172ce632276217ce992` (`Make kitchen order changes prominent`); the authorized direct-main workflow required no separate pull request or merge. Verify FOND run `35718512109` passed in 2m22s, Deploy FOND Staging run `35718723712` passed in 51s, and Brett explicitly authorized Promote FOND Production run `35723882702`, which passed in 46s for that exact verified commit. Deployment changed no menu data, provider credential, payment mode or notification setting.

Live production verification: `https://fond.mid-point.co.za/api/health` returned `status=ok` with `payments=yoco-sandbox`, and `/staff` returned HTTP 200. The released staff assets contain `CHANGES TO MAKE`, `CUSTOMER NOTE` and the `data-change-kind` styling hook, proving the production bundle includes the labelled kitchen-change panels. No production order, payment, email, SMS or WhatsApp message was created, and no live order state was changed. Physical-tablet readability with a controlled modified order remains the operational UAT gate. No Asana tracker is in scope.
