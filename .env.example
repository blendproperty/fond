# Confirm the actual Hostinger VPS routing values before deployment.
FOND_HOST=
TRAEFIK_NETWORK=
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERTRESOLVER=
# No Yoco secrets are needed for the demo. Never use NEXT_PUBLIC_ for secrets.
